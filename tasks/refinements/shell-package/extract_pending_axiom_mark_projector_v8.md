# Defer the `projectPendingAxiomMarks` lift into `@a-conversa/shell` (v8) — both preconditions remain unmet; cadence-rule pickup signals unmet for the second consecutive iteration; the `_v7` cadence-enforcement-revisit clause fires; close and re-register `_v9` with a tj3-enforceable cadence-gate task

**TaskJuggler entry**: [tasks/27-shell-package.tji](../../27-shell-package.tji) — task `shell_package.extract_pending_axiom_mark_projector_v8` (lines 324-373).
**Effort estimate**: 0.5d (WBS budget — the precondition-audit deliverable plus the **second-consecutive** cadence-rule-violation observation plus the escalation decision plus the same-trigger `_v9` re-registration with a tj3-enforceable cadence-gate task consume ~0.35d together; the lift itself does not land until both UI triggers fire).

## Inherited dependencies

- `shell_package.extract_pending_axiom_mark_projector_v7` (settled 2026-05-29, commit `2debcc2` — [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v7.md`](extract_pending_axiom_mark_projector_v7.md)). **Source of debt.** The strict `!`-edge predecessor and the leaf that pre-registered THIS task verbatim (its Decision §4): same name, same 0.5d effort, same binary lift trigger (*"once BOTH a participant pending-axiom-mark rendering leaf AND an audience pending-axiom-mark rendering leaf are complete 100"*), AND the iteration that first observed the cadence-rule-violation pattern (its Decision §3: prose-only cadence in `.tji` `note` blocks has no `tj3`-enforceable scheduling effect; the orchestrator's pick-task pass walks `complete 100` markers and `depends` edges only, so the batched-audit cadence was unhonored when `_v7` was picked at pick-pass 1 with signal (ii) also unfired). The same Decision §3 named two pre-described escalation paths — (1) a `tj3`-enforceable cadence-gate task, (2) a non-leaf "watcher" pattern requiring a brief amendment to the orchestrator's pick-task workflow — but **declined to escalate on a single observation** per the v3 precedent's two-iteration evidence-window discipline. Its Decision §4 added a **cadence-enforcement-revisit clause** to the `_v8` registration: *"if `_v8`'s pickup also violates the cadence (pick-pass-1 or pick-pass-2 pickup with signal (ii) unmet — i.e., a second consecutive cadence violation), `_v8`'s refinement re-evaluates whether to escalate the cadence enforcement mechanism per the two paths named in Decision §3."* This `_v8` audit fires that clause: the orchestrator's pick-task pass surfaced `_v8` at pick-pass 1 with signal (ii) also unfired — a second consecutive violation. The pattern is now confirmed across two iterations and the escalation decision is the new substance of this refinement (Decision §3 below).
- `shell_package.extract_pending_axiom_mark_projector_v6` (settled 2026-05-29, commit `42ad04b` — [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v6.md`](extract_pending_axiom_mark_projector_v6.md)). The iteration that closed the v3-named cadence-revisit-evidence window and adopted the batched-audit pickup cadence for `_v7` (its Decision §3 + §4): pick when (i) 4 or more orchestrator-pick passes have elapsed since `_v6` close, OR (ii) a commit lands touching one of the precondition-bearing files with a matching pending-axiom-mark identifier. Inherited transitively through `_v7`; cited directly here because its Decision §3 + §4 is the cadence-rate the `_v8` cadence-violation observation is measured against and the enforcement-revisit clause inherits.
- `shell_package.extract_pending_axiom_mark_projector_v5` (settled 2026-05-29, commit `b38afc2` — [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v5.md`](extract_pending_axiom_mark_projector_v5.md)). The first iteration of the v3-named cadence-revisit-evidence window. Inherited transitively through `_v6` → `_v7`.
- `shell_package.extract_pending_axiom_mark_projector_v4` (settled 2026-05-29, commit `75c784a` — [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v4.md`](extract_pending_axiom_mark_projector_v4.md)). Inherited transitively through `_v5` → `_v6` → `_v7`.
- `shell_package.extract_pending_axiom_mark_projector_v3` (settled 2026-05-29, commit `e5d9147` — [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v3.md`](extract_pending_axiom_mark_projector_v3.md)). The iteration that established the **two-iteration evidence-window discipline** (its Decision §4) the `_v6` closure acted on and the `_v7` Decision §3 + this `_v8` Decision §3 each inherit for cadence-policy decisions. The v3 precedent's load-bearing rule — "a single iteration is observation, not enough evidence for unilateral policy change; tighten only after the pattern repeats across a two-iteration window" — is the exact rule that justifies escalating cadence-enforcement at `_v8` (the second iteration of the prose-only-violation pattern), having declined to escalate at `_v7`. Inherited transitively through `_v4` → `_v5` → `_v6` → `_v7`; cited here directly because its evidence-window discipline is the rule the `_v8` escalation decision applies.
- `shell_package.extract_pending_axiom_mark_projector_v2` (settled 2026-05-28, commit `9733b28` — [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v2.md`](extract_pending_axiom_mark_projector_v2.md)). The v2 audit. Inherited transitively through `_v3` → `_v4` → `_v5` → `_v6` → `_v7`.
- `shell_package.extract_pending_axiom_mark_projector` (settled 2026-05-28, commit `683143f` — [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector.md`](extract_pending_axiom_mark_projector.md)). The v1 audit — origin of the audit-iteration chain; Decisions §1-§3 framed the audit-and-defer disposition; Decision §4 registered `_v2`. Inherited transitively through `_v2` → `_v3` → `_v4` → `_v5` → `_v6` → `_v7`.
- `shell_package.extract_votes_by_facet_projector_v2` (settled 2026-05-28, commit `8c7087b` — [`tasks/refinements/shell-package/extract_votes_by_facet_projector_v2.md`](extract_votes_by_facet_projector_v2.md)). The sibling `_v2` lift that DID fire — **reference precedent for what a `_vN` lift looks like when its preconditions DO get met.** The asymmetry with this chain is unchanged at `_v8` audit time and continues to be the durable reason this chain's `_vN` continues to defer while the votes-by-facet `_v2` terminated by lifting. v2 + v3 + v4 + v5 + v6 + v7 Decisions §1 already recorded this asymmetry in full; this `_v8` inherits the rejection verbatim. Not a `.tji` `depends` edge — prose-only authority.
- Prose-only context (NOT a `.tji` edge): `shell_package.extract_facet_pill` (settled — [`tasks/refinements/shell-package/extract_facet_pill.md`](extract_facet_pill.md)) — source of the three-caller policy. One- and two-caller helpers stay per-workspace; lift fires when the third caller materializes with a convergent shape. `projectPendingAxiomMarks` has one caller (moderator-only) at `_v8` commit time; the gap to the three-caller floor is unchanged from `_v7` audit time.
- Prose-only context (NOT a `.tji` edge): `shell_package.extract_cytoscape_projectors` (settled — [`tasks/refinements/shell-package/extract_cytoscape_projectors.md`](extract_cytoscape_projectors.md)) — pins the lift recipe (`packages/shell/src/<vocabulary>/` sibling directory, consolidated Vitest suite, root re-export block, atomic-move-no-shim per-workspace import rewire) that the future `_v9+N` (or terminating) lift will follow verbatim when the trigger fires.
- Prose-only context (NOT a `.tji` edge): `shell_package.shell_axiom_marks_extraction` (settled — [`tasks/refinements/shell-package/shell_axiom_marks_extraction.md`](shell_axiom_marks_extraction.md)) — establishes `packages/shell/src/axiom-marks/` as the canonical home for the committed projector trio, AND the sibling-directory convention this audit chain's future lift would inherit (`packages/shell/src/pending-axiom-marks/`). Pending and committed variants live as separate sibling directories, not as a merged namespace (v1 Decision §1 alternative C rejected — inherited through `_v2` → `_v3` → `_v4` → `_v5` → `_v6` → `_v7` unchanged).
- Prose-only context (NOT a `.tji` edge): `moderator_ui.mod_axiom_mark_pending_render` (settled — [`tasks/refinements/moderator-ui/mod_axiom_mark_pending_render.md`](../moderator-ui/mod_axiom_mark_pending_render.md)) — the moderator leaf that landed the four pending-axiom-mark symbols. Decision §1 documents the two-terminator handling (`commit` + `meta-disagreement-marked`, both with `payload.target === 'proposal'`); Decision §2 the no-per-participant-uniqueness rule; Decision §4 scoped the trio as moderator-internal at authoring time, coupled to the moderator's proposal-author affordance.
- Prose-only context (NOT a `.tji` edge): `participant_ui.part_axiom_mark_decoration` (settled — [`tasks/refinements/participant-ui/part_axiom_mark_decoration.md`](../participant-ui/part_axiom_mark_decoration.md)) — **explicitly excludes pending-axiom-mark rendering** ([`:27`](../participant-ui/part_axiom_mark_decoration.md#L27)); at-a-glance signal is "ratified bedrock"; pending is deferred to a hypothetical `part_pending_proposals.*` group. The `part_pending_proposals.*` group exists in the WBS ([`tasks/40-participant-ui.tji:210`](../../40-participant-ui.tji#L210)) but its shipped leaves landed a list-view pane, NOT a graph-canvas pending-axiom-mark overlay — the principled-absence is unrevised at `_v8` audit time. The sibling subtree `part_axiom_mark_from_tablet` (lines 208-245) is `complete 100` for **committed**-axiom-mark workflow only; its deferred-∞ sub-tasks (`part_mark_axiom_action`, `part_axiom_mark_proposal`) name pending-mark **authoring** as future work but not pending-mark **rendering**.
- Prose-only context (NOT a `.tji` edge): `audience.aud_axiom_mark_decoration` (settled — [`tasks/refinements/audience/aud_axiom_mark_decoration.md`](../audience/aud_axiom_mark_decoration.md)) — **explicitly excludes pending-axiom-mark rendering** ([`:38`](../audience/aud_axiom_mark_decoration.md#L38)); a future polish leaf (`aud_axiom_mark_pending_render`, ~1d) is named in prose but explicitly **NOT pre-registered** ("registered only when a concrete trigger materializes, not on a calendar date"). The principled-absence is unrevised at `_v8` audit time. The sibling subtree `audience.aud_graph_rendering` is 30/52 `complete 100`; the remaining leaves include `aud_pending_proposals_pane` and `aud_pending_proposals_pending_votes` (deferred), but no leaf scopes pending-axiom-mark **graph-canvas** rendering.
- Prose-only context (NOT a `.tji` edge): ADR 0030 §9 (per-facet vote keying) — the structural sub-kinds of `target='proposal'` vs `target='facet'` that the projector's two terminator branches resolve through. The proposal-keyed terminator arm's authority is unchanged by this audit and would be honored verbatim by the future lift.

## What this task is

The half-day precondition-audit AND second-consecutive-cadence-violation-observation AND **cadence-enforcement-escalation-decision** AND same-trigger `_v9` re-registration deliverable for the **still-deferred** `projectPendingAxiomMarks` shell-extraction, at audit-iteration 8. The predecessor `_v7` audit ([`extract_pending_axiom_mark_projector_v7.md`](extract_pending_axiom_mark_projector_v7.md), settled 2026-05-29 at commit `2debcc2`) registered THIS `_v8` task with the unchanged binary lift trigger ("both UI leaves `complete 100`") AND the same batched-audit pickup cadence as `_v7` (its Decision §4: pick when (i) 4 or more orchestrator-pick passes have elapsed since `_v7` close, OR (ii) a commit lands touching one of the precondition-bearing files with a matching pending-axiom-mark identifier) AND a **cadence-enforcement-revisit clause**: if `_v8`'s pickup also violates the cadence (pick-pass-1 or pick-pass-2 pickup with signal (ii) unmet — i.e., a second consecutive violation), this refinement re-evaluates whether to escalate the cadence enforcement mechanism per `_v7` Decision §3's two pre-named escalation paths. The orchestrator's pick-task pass surfaced this `_v8` task on the first pick after `_v7` closed — a state in which both cadence signals are unmet (signal (i): 1 pass elapsed against a 4-pass ceiling; signal (ii): no commit between `2debcc2` and this `_v8` commit has touched any precondition-bearing file with a matching identifier). The pattern of "prose-only cadence is unhonored by the orchestrator's mechanical pick-task pass" is now confirmed across two consecutive iterations (`_v7` observation + `_v8` observation); the cadence-enforcement-revisit clause therefore fires.

This refinement audits the lift trigger at refinement time and confirms **both preconditions remain unmet AND remain principledly absent**, identically to the `_v7` finding one orchestrator-pick pass earlier. The findings are bit-for-bit identical to `_v7`'s, because the only commit landed between `_v7`'s close commit (`2debcc2`) and this `_v8` audit pass is `_v7`'s own close commit (which touches [`tasks/27-shell-package.tji`](../../27-shell-package.tji) and [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v7.md`](extract_pending_axiom_mark_projector_v7.md) — neither in the cadence-(ii) signal's precondition-bearing file list, by design):

- **Precondition (a) — participant pending-axiom-mark rendering leaf — unmet, unchanged.** A grep of [`apps/participant/src/`](../../../apps/participant/src/) for `pendingAxiomMark` / `PendingAxiomMark` / `pending-axiom-mark` / `projectPendingAxiomMarks` / `EMPTY_PENDING_AXIOM_MARKS` at `_v8` refinement time returns **zero matches** — no participant code imports, consumes, or re-implements the projector. A grep of [`tasks/40-participant-ui.tji`](../../40-participant-ui.tji) for `pending_axiom` / `axiom_mark_pending` / `axiom_pending` returns **zero matches**: no task identifier names a participant pending-axiom-mark rendering leaf, complete or otherwise. The lone `part_pending_proposals` task ([`:210`](../../40-participant-ui.tji#L210)) is a list-view pane, not a graph-canvas pending-axiom-mark overlay. [`part_axiom_mark_decoration.md:27`](../participant-ui/part_axiom_mark_decoration.md#L27) reads the same line at the same offset as it did at `_v7` audit time; the refinement's Status line ("Done — 2026-05-17") still has no amendment. The participant's principled-absence decision (committed-only "ratified bedrock") has not been revised. The trigger cannot fire until a new participant leaf scopes graph-canvas pending-axiom-mark rendering; no such leaf exists.
- **Precondition (b) — audience pending-axiom-mark rendering leaf — unmet, unchanged.** A grep of [`apps/audience/src/`](../../../apps/audience/src/) for `pendingAxiomMark` / `PendingAxiomMark` / `pending-axiom-mark` returns **zero matches** — no audience code imports, consumes, or re-implements the projector. A grep of [`tasks/50-audience-and-broadcast.tji`](../../50-audience-and-broadcast.tji) for `aud_axiom_mark_pending` / `aud_pending_axiom_mark` / `pending_axiom_mark` returns **zero matches**. The deferred leaves under `audience.aud_graph_rendering` (`aud_pending_proposals_pane`, `aud_pending_proposals_pending_votes`) scope a future pending-proposals pane but not pending-axiom-mark **graph-canvas** rendering. [`aud_axiom_mark_decoration.md:38`](../audience/aud_axiom_mark_decoration.md#L38) is byte-identical to its `_v7` audit-time content; the prose-named future polish leaf (`aud_axiom_mark_pending_render`, ~1d) remains explicitly NOT pre-registered. The refinement's Status line ("Done — 2026-05-28") still has no amendment. The audience's principled-absence decision is unrevised. The trigger cannot fire until a new audience leaf scopes pending-axiom-mark rendering; no such leaf exists.

Both surfaces have **continuously, identically, and independently maintained** their committed-only "ratified bedrock" framing across the v1 → v2 → v3 → v4 → v5 → v6 → v7 → v8 audit interval. The only commit landed between `_v7` close (`2debcc2`) and this `_v8` audit is `_v7`'s own close commit; the moderator's four pending-axiom-mark symbols remain at byte-identical line offsets in [`apps/moderator/src/graph/selectors.ts`](../../../apps/moderator/src/graph/selectors.ts) (line 212 `PendingAxiomMark`, line 225 `EMPTY_PENDING_AXIOM_MARKS`, line 251 `projectPendingAxiomMarks`, line 303 `groupPendingAxiomMarksByNode`). The principled-absence the v1 → v7 audits all recorded as the reason for deferral is structurally unchanged.

Four deliverables sit inside this leaf:

1. The audit record itself: a confirmation that the deferral remains correct one further audit-iteration on, that the trigger semantics are unchanged, and that the moderator's four-symbol module-internal state is preserved bit-for-bit since `_v7`.
2. **The second-consecutive cadence-rule-violation observation.** The batched-audit cadence (every 4 pick-passes OR a precondition-touching commit) was not honored by the orchestrator's pick-task pass — `_v8` was picked at pick-pass 1, with signal (ii) also unfired. Combined with the identical `_v7` observation, the pattern is now confirmed across the two-iteration evidence window the `_v3` precedent's discipline demands. Decision §3 records the confirmation.
3. **The cadence-enforcement escalation decision.** The `_v7` Decision §4 cadence-enforcement-revisit clause fires. Decision §3 evaluates `_v7` Decision §3's two pre-named escalation paths (a `tj3`-enforceable cadence-gate task; a non-leaf "watcher" pattern) plus a third "hold prose-only" alternative, and **chooses path (1) — a `tj3`-enforceable cadence-gate task** for `_v9` (framed as the orchestrator brief's "formal blocking dependency added to the WBS" enforcement mechanism). Path (2) is rejected because it requires amending the orchestrator's pick-task workflow, which is outside this audit chain's scope; "hold prose-only" is rejected because the v3 precedent's discipline now demands action.
4. **The same-trigger `_v9` re-registration with a sibling `_v9_cadence_gate` task.** Decision §4 registers two named-future-tasks: `shell_package.extract_pending_axiom_mark_projector_v9` (the audit-iteration leaf, depending on both `!_v8` AND the new cadence-gate sentinel) AND `shell_package.extract_pending_axiom_mark_projector_v9_cadence_gate` (a milestone-style sentinel task whose `complete 100` is gated by deliberate human flip — by the closer or the orchestrator — only when one of the named cadence signals fires). The cadence-gate task is the structural enforcement mechanism: the orchestrator's pick-task pass walks `complete 100` + `depends` edges only, and with the gate `complete 0`, `_v9` is mechanically unpickable until the gate flips. The lift-trigger semantics (both UI leaves `complete 100`) are unchanged at `_v9`.

After this leaf:

- [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v8.md`](extract_pending_axiom_mark_projector_v8.md) (this file) exists as the audit record AND the second-consecutive cadence-rule-violation observation AND the escalation-decision record AND the `_v9` + gate-task registration record.
- The four moderator-local symbols stay where they are: [`apps/moderator/src/graph/selectors.ts`](../../../apps/moderator/src/graph/selectors.ts) — `PendingAxiomMark` interface (line 212), `EMPTY_PENDING_AXIOM_MARKS` (line 225), [`projectPendingAxiomMarks` at line 251](../../../apps/moderator/src/graph/selectors.ts#L251), [`groupPendingAxiomMarksByNode` at line 303](../../../apps/moderator/src/graph/selectors.ts#L303). Byte-identical to the `_v7` audit-time snapshot.
- The WBS task `shell_package.extract_pending_axiom_mark_projector_v8` is marked `complete 100` by the closer (the audit + the second-consecutive cadence-observation + the escalation decision + the two-task `_v9` registration are the deliverable for this leaf's budget). The closer registers TWO follow-up named-future-tasks in [`tasks/27-shell-package.tji`](../../27-shell-package.tji) — `shell_package.extract_pending_axiom_mark_projector_v9` AND its `_v9_cadence_gate` sentinel — with the gate's `complete 0` status and flip-protocol documented in its own `note` block.
- The next time the precondition trigger appears to fire (a participant or audience pending-axiom-mark rendering refinement reaches its source-of-debt stage), OR the next time the cadence-gate sentinel is deliberately flipped to `complete 100` (per the flip-protocol named in Decision §4), the implementer reads this audit first and acts on the named-future-task `_v9` — not on this `_v8` leaf, which is closed.

Out of scope (explicitly NOT done here):

- **The lift itself.** No `packages/shell/src/pending-axiom-marks/` directory is created. No symbol is moved. No callers rewire imports. The shell substrate gains no new entry. The shell's existing `packages/shell/src/axiom-marks/` directory (the committed-mark sibling) is untouched; in particular, the pending and committed variants are NOT merged into a single namespace (v1 Decision §1 alternative C rejected — inherited through `_v2` → `_v3` → `_v4` → `_v5` → `_v6` → `_v7` → here unchanged).
- **Relaxing the three-caller policy on the strength of "the methodology question is settled in the moderator-only direction."** Already rejected by `_v2` + `_v3` + `_v4` + `_v5` + `_v6` + `_v7` Decision §1 (alternative B) under the votes-by-facet-asymmetry rationale. The asymmetry is unchanged at `_v8` audit time; the rejection is inherited verbatim (Decision §1 of this refinement records the inheritance).
- **Pre-registering the participant `part_pending_proposals.*` graph-canvas overlay leaf or the audience `aud_axiom_mark_pending_render` leaf.** Both surfaces' refinements explicitly declined to register those leaves (v1 + v2 + v3 + v4 + v5 + v6 + v7 Decision §2, inherited verbatim). Pre-registering them from a shell-package `_v8` audit would invert the WBS's dependency direction. Rejected as alternative B in Decision §2.
- **A parametric / merged-axiom-mark projector** (`projectAxiomMarks` extended to optionally produce pending entries via an options bag). Already rejected by v1 Decision §1 alternative C; inherited through `_v2` → `_v3` → `_v4` → `_v5` → `_v6` → `_v7` to here.
- **`<PendingAxiomMarkBadge>` component itself.** Out of this leaf's name list (the WBS scope is the projector trio + the type + the empty-array constant — not the badge component). Out of scope here; not registered as a named-future-task today.
- **Changes to the moderator's projector behavior or its `<PendingAxiomMarkBadge>` consumer.** The four moderator-local symbols and their three consumers ([`GraphCanvasPane.tsx`](../../../apps/moderator/src/graph/GraphCanvasPane.tsx), [`StatementNode.tsx`](../../../apps/moderator/src/graph/StatementNode.tsx), [`PendingAxiomMarkBadge.tsx`](../../../apps/moderator/src/graph/PendingAxiomMarkBadge.tsx)) stay byte-identical at this commit. No re-comment, no signature change, no `Readonly` widen, no extraction of an `EventLog`-style adapter — same constraint as v1 + v2 + v3 + v4 + v5 + v6 + v7.
- **A unification of the committed and pending Cucumber surfaces or any new BDD pin.** No Cucumber scenario lands. No protocol seam is crossed. The audit is documentation-only.
- **`projectVotesByProposal`** ([`selectors.ts`](../../../apps/moderator/src/graph/selectors.ts)) and other moderator-only single-caller helpers. Already explicitly out of scope per the v1 → v7 audits. Not re-litigated here.
- **Choosing the "non-leaf watcher" escalation path (path (2) from `_v7` Decision §3).** Decision §3 rejects this path: it requires amending the orchestrator's pick-task workflow (a meta-policy change beyond this audit chain's scope) and introduces an abstraction whose value is amortized only across multiple chains adopting similar policies — neither of which is justified by this single-chain cadence-violation pattern.
- **Choosing "hold prose-only" as a third alternative at `_v9` registration.** The cadence-enforcement-revisit clause was specifically designed to act on confirmed evidence; holding prose-only after the two-iteration discipline has confirmed the pattern would violate the v3 precedent's evidence-gathering rule and would render the cadence-enforcement-revisit clause vestigial.
- **Binding the cadence-gate flip to a wall-clock calendar date** (e.g., "flip the gate on 2026-08-01"). Already rejected by `_v5` Decision §3 alternative D, `_v6` Decision §3 alternative D, and `_v7` (per its inheritance); inherited verbatim. The gate's flip-protocol is signal-based (signal (i): elapsed-pick-pass count; signal (ii): precondition-touching commit), not calendar-based.
- **Restructuring the WBS so the cadence-gate is `tj3`-enforced via `start`/`length`/`duration` constructs.** Those constructs bind to wall-clock dates and would reintroduce the calendar-binding rejected above. The gate is a milestone-style sentinel with `complete 0` until deliberate human flip; its enforcement is the orchestrator's pick-task pass refusing to surface `_v9` while the gate's `depends` edge is open.
- **Writing an ADR for the cadence-gate mechanism.** Per `_v7` Decision §1 inherited reasoning: this is a WBS-cadence policy applied to one audit chain, not an architectural decision affecting the codebase. If a second audit chain adopts the same gate mechanism (cross-chain reuse) or if the gate's flip-protocol introduces a meta-policy seam (e.g., the orchestrator's pick-task workflow needs to be amended to scan for gates), THAT work's refinement may amend the relevant ADR or open a new one; this audit does not.

## Why it needs to be done

The `_v7` audit closed with the explicit pre-registration of THIS `_v8` task at unchanged lift-trigger semantics, the same batched-audit pickup cadence as `_v7`, AND a **cadence-enforcement-revisit clause** scoped specifically to fire on a second consecutive cadence violation (see `_v7` Decision §3 + §4). The orchestrator's pick-task pass surfaced this task on the first pick after `_v7` closed — a state in which neither cadence signal had fired, exactly mirroring the `_v7` pickup-state. The pass cannot verify either (i) whether the participant + audience leaves have materialized between `_v7`'s commit (`2debcc2`, 2026-05-29) and this `_v8`'s commit, or (ii) whether the cadence-enforcement-revisit clause is fired by the second-consecutive violation pattern. Both verifications, plus the escalation decision the verified-clause demands, plus the resulting `_v9` re-registration with a gate-task structure, are the refinement-stage activity this leaf delivers.

Six observations make this audit valuable (rather than a no-op formality at one further iteration's distance from the originating v1):

1. **The `_v7` audit identified the same revision path the v1 → v6 audits named (the participant and audience refinements would have to explicitly revise their principled-absence decisions), and one further audit-iteration is the right place to re-check whether that path has been walked.** A search of the two work-streams' refinements + their `.tji` task blocks at `_v8` commit time shows zero revisions: [`part_axiom_mark_decoration.md`](../participant-ui/part_axiom_mark_decoration.md) Status line says "Done — 2026-05-17" with no amendments; [`aud_axiom_mark_decoration.md`](../audience/aud_axiom_mark_decoration.md) Status line says "Done — 2026-05-28" with no amendments. No new participant leaf scoping graph-canvas pending-axiom-mark rendering; no new audience leaf scoping `aud_axiom_mark_pending_render` or any analog; no commits touching either surface's stance on pending-axiom-marks (the only commit between `2debcc2` and this `_v8` audit is `2debcc2` itself, the `_v7` close). The principled-absence is durable across eight audit iterations, not transient.
2. **The sibling `extract_votes_by_facet_projector_v2` lift remains the load-bearing asymmetry citation.** The v2 → v7 audits' Decision §1 each recorded in full why that `_v2` lifted (in-flight methodology task closed the divergence half; two byte-identical projector bodies across two existing callers gave evidence weight to relaxing the third-workspace half) and why this audit chain's pending-axiom-mark `_v2` → `_v7` did NOT (the methodology is settled in the principled-absence direction; the trigger is genuinely binary on UI work that is not scheduled). The asymmetry is unchanged at `_v8` audit time and continues to be the durable reason this chain's `_vN` continues to defer while the votes-by-facet `_v2` terminated by lifting. Recording the asymmetry inline saves a future reader from re-deriving the methodology context from the source refinements.
3. **Closing this `_v8` leaf cleanly with a tj3-enforceable cadence-gate prevents the third consecutive pick-task queue churn while resolving the cadence-rule-violation pattern.** Leaving the leaf open means every pick-task pass either re-refines it or skips it ambiguously. The closer marks the leaf `complete 100`; the future-trigger work is held by a freshly-named `_v9` follow-up with a structural-enforcement gate (Decision §4). The orchestrator's pick-task pass will mechanically skip `_v9` until the gate flips — closing the loop the prose-only cadence could not close.
4. **The two-iteration evidence window the v3 precedent's discipline named is now closed in the affirmative direction.** v3 Decision §4 named the cadence-policy decision rate as "across a two-iteration evidence window, NOT at any single iteration." The same discipline applies to **how** the new cadence's enforcement is evaluated. `_v7` Decision §3 observed the first violation and explicitly deferred the escalation question to `_v8` "if the pattern repeats" — which it has. `_v8` Decision §3 is the right place to act because the evidence-window has closed with the same observation in both iterations: prose-only cadence in `.tji` `note` blocks has no `tj3`-enforceable scheduling effect; the orchestrator's pick-task pass walks `complete 100` + `depends` edges only. The pattern is not iteration-specific noise but a structural property of the orchestrator's mechanical scheduler. Acting at the second iteration honors the precedent's evidence-gathering rate without over-correcting.
5. **The escalation path chosen (a `tj3`-enforceable cadence-gate task) is the orchestrator brief's "formal blocking dependency added to the WBS" mechanism applied concretely.** The orchestrator's escalation-revisit clause inherited from `_v7` named three alternative enforcement mechanisms: (a) a tj3 milestone gate, (b) a CI check, (c) a formal blocking dependency added to the WBS. Paths (a) and (c) collapse into the same concrete mechanism inside `tj3` (a sentinel task with `complete 0` until human flip; the `depends` edge on the sentinel is what blocks `_v9`); path (b) is out of scope (CI infrastructure for cadence enforcement is meta-policy work the orchestrator's pick-task workflow would amend, not a single-chain refinement decision). Choosing path (a)/(c) keeps the enforcement entirely inside `tj3`'s pick-task pass semantics, which is what the orchestrator's pick-task pass actually consults.
6. **Recording the cadence-gate flip-protocol crisply produces durable enforcement.** Decision §4 names two flip-signals (the same (i) and (ii) signals from `_v7`/`_v6` cadence policy) and binds the gate's `complete 100` to deliberate human verification of one of those signals firing. This means: the closer (or the orchestrator, in a pick-task pass) actively checks signals before flipping the gate, instead of the orchestrator's pick-task pass mechanically picking the next leaf because the predecessor closed. The check-then-flip protocol is the structural enforcement; it cannot be bypassed silently because the gate's `depends` edge is open until the flip happens.

The follow-on benefits of the audit:

- **Prevents premature ossification.** The three-caller policy's whole point is to wait for empirical convergence rather than to design-shape-and-hope. Lifting `projectPendingAxiomMarks` into the shell today as a single-caller move "in case" the second and third callers eventually materialize would create a shell-side projector whose only consumer is the moderator's `GraphCanvasPane` — exactly the anti-pattern the v1 → v7 audits rejected. The `_v8` re-audit confirms the reasoning still holds.
- **Records the named-future-task path crisply with the new enforcement going forward.** The orchestrator brief's tech-debt registration policy asks every deferred task to be named with effort + description; Decision §4 supplies both for the `_v9` follow-up AND the new `_v9_cadence_gate` sentinel, freshly stating the trigger semantics AND the gate's `complete 0` status AND the flip-protocol. The deferral remains visible on the WBS, AND the orchestrator's pick-task pass will mechanically respect it.
- **Pins the recipe in advance.** When the trigger does fire (likely 2-4 quarters out, after the participant's pending-proposals pane expands or the audience's broadcast-feedback iteration surfaces a need), the implementer of the terminating-lift refinement reads the audit chain and follows the `shell_axiom_marks_extraction` + `extract_cytoscape_projectors` + `extract_votes_by_facet_projector_v2` recipe verbatim — no design re-derivation needed across the chain.
- **Names the gate-task pattern for cross-chain reuse without prematurely abstracting it.** Decision §3 names the gate-task structure narrowly (one sentinel per audit chain, named per-chain — e.g., `_v9_cadence_gate`) so other audit chains observing the same prose-only-cadence-unhonored pattern can adopt the same shape without a meta-abstraction landing prematurely. If two or more chains adopt the gate pattern, a future refinement may consolidate the gate semantics into a shared convention (potentially an ADR); this audit does not pre-abstract.

## Inputs / context

### ADRs

- [ADR 0022 — No throwaway verifications](../../../docs/adr/0022-no-throwaway-verifications.md). The Vitest layer for the pending-axiom-mark projector stays where it is — the moderator's [`apps/moderator/src/graph/selectors.test.ts`](../../../apps/moderator/src/graph/selectors.test.ts) cases at the `describe('projectPendingAxiomMarks')` and `describe('groupPendingAxiomMarksByNode')` blocks are the regression pin for the moderator surface's local behavior. No throwaway smoke script is introduced here; this audit produces no executable artifact.
- [ADR 0026 — Micro-frontend root app](../../../docs/adr/0026-micro-frontend-root-app.md). When the lift does fire (`_v9` or beyond), the shell's `packages/shell/src/pending-axiom-marks/` directory is the architecturally-correct home per the ADR's substrate posture. Today the audit defers that move; the substrate-posture rationale stays intact for the future lift.
- [ADR 0030 — Per-facet vote keying](../../../docs/adr/0030-per-facet-vote-keying.md). §9 (the structural sub-kinds of `target='proposal'` vs `target='facet'`) is what the projector's two terminator branches resolve through ([`apps/moderator/src/graph/selectors.ts:278,290`](../../../apps/moderator/src/graph/selectors.ts#L278) — commit + meta-disagreement-marked `target='proposal'` events remove pending entries); the proposal-keyed terminator arm is bound by this ADR. Unchanged by this audit; bound for the future lift to honor verbatim.

### Prior shell-package decisions this audit is bound by

- [`extract_pending_axiom_mark_projector_v7.md`](extract_pending_axiom_mark_projector_v7.md) — the immediately-preceding audit. Decisions §1-§4 frame the audit-and-defer disposition, the no-pre-registration policy, the first cadence-rule-violation observation with two pre-named escalation paths, and the `_v8` registration shape (with the cadence-enforcement-revisit clause). This `_v8` audit honors §1, §2, and §4 verbatim — same audit shape, same close-and-defer disposition for the lift trigger, same crisp follow-up registration shape (now for `_v9` + the cadence-gate sentinel). §3 (the cadence-rule-violation observation + the two pre-named escalation paths + the deferred escalation decision) is the rule this `_v8`'s Decision §3 acts on: the second-consecutive observation closes the two-iteration evidence window, and `_v8` chooses path (1) — a `tj3`-enforceable cadence-gate task.
- [`extract_pending_axiom_mark_projector_v6.md`](extract_pending_axiom_mark_projector_v6.md) — the predecessor of `_v7`; the iteration that scoped the batched-audit cadence the `_v7` and `_v8` cadence-violation observations are measured against. Inherited transitively through `_v7`; cited directly because the cadence-rate is unchanged by this audit (only the enforcement mechanism is being escalated).
- [`extract_pending_axiom_mark_projector_v5.md`](extract_pending_axiom_mark_projector_v5.md) — the first iteration of the v3-named cadence-revisit-evidence window. Inherited transitively through `_v6` → `_v7`.
- [`extract_pending_axiom_mark_projector_v4.md`](extract_pending_axiom_mark_projector_v4.md) — the v4 audit. Inherited transitively through `_v5` → `_v6` → `_v7`.
- [`extract_pending_axiom_mark_projector_v3.md`](extract_pending_axiom_mark_projector_v3.md) — the v3 audit; the iteration that established the **two-iteration evidence-window discipline** Decision §3 of this refinement applies. Inherited transitively through `_v4` → `_v5` → `_v6` → `_v7`; cited directly because its precedent is the load-bearing rule this `_v8` escalation decision turns on.
- [`extract_pending_axiom_mark_projector_v2.md`](extract_pending_axiom_mark_projector_v2.md) — the v2 audit. Inherited transitively through `_v3` → `_v4` → `_v5` → `_v6` → `_v7`.
- [`extract_pending_axiom_mark_projector.md`](extract_pending_axiom_mark_projector.md) — the v1 audit. Decisions §1-§3 framed the audit-and-defer disposition; Decision §4 pre-registered `_v2`. Inherited transitively through `_v2` → `_v3` → `_v4` → `_v5` → `_v6` → `_v7`.
- [`extract_votes_by_facet_projector_v2.md`](extract_votes_by_facet_projector_v2.md) Decision §1 — the lift-relaxation precedent. Pending-axiom-marks structurally do not match (recorded in `_v2` → `_v7` Decision §1; inherited here verbatim).
- [`extract_cytoscape_projectors.md`](extract_cytoscape_projectors.md) Decision §3 — the original source-of-debt for the v1 audit, inherited transitively.
- [`extract_facet_pill.md`](extract_facet_pill.md) Decision §2 — the three-caller policy.
- [`shell_axiom_marks_extraction.md`](shell_axiom_marks_extraction.md) — the directory + barrel + root-export shape the future lift will inherit.

### Prior moderator + participant + audience decisions on pending-axiom-marks

- [`mod_axiom_mark_pending_render.md`](../moderator-ui/mod_axiom_mark_pending_render.md) Decisions §1 (two-terminator handling), §2 (no per-participant uniqueness), §4 (pending-projector trio scoped as moderator-internal — coupled to the moderator's proposal-author affordance).
- [`part_axiom_mark_decoration.md`](../participant-ui/part_axiom_mark_decoration.md) [`:27`](../participant-ui/part_axiom_mark_decoration.md#L27) — explicit exclusion of pending-axiom-mark rendering from the participant surface; at-a-glance signal is "ratified bedrock" only. Stance unchanged at `_v8` commit time (Status line: Done — 2026-05-17; no amendments).
- [`aud_axiom_mark_decoration.md`](../audience/aud_axiom_mark_decoration.md) [`:38`](../audience/aud_axiom_mark_decoration.md#L38) — explicit exclusion of pending-axiom-mark rendering from the audience surface; future polish leaf `aud_axiom_mark_pending_render` (~1d) named in prose but explicitly NOT pre-registered. Stance unchanged at `_v8` commit time (Status line: Done — 2026-05-28; no amendments).

### Live code at refinement time

#### Moderator surface — canonical (and only) pending-axiom-mark projector; unchanged since `_v7`

[`apps/moderator/src/graph/selectors.ts`](../../../apps/moderator/src/graph/selectors.ts) defines the same four contiguous symbols the `_v7` audit recorded, at the same line offsets:

- `PendingAxiomMark` interface at line 212 (the readonly `{ proposalEventId, nodeId, participantId, proposedAt }` shape).
- `EMPTY_PENDING_AXIOM_MARKS: readonly PendingAxiomMark[]` at line 225 (the frozen-empty reference for per-node fallbacks).
- [`projectPendingAxiomMarks(events) → PendingAxiomMark[]` at line 251](../../../apps/moderator/src/graph/selectors.ts#L251) — walks events once with a `Map<proposalEventId, PendingAxiomMark>`; records on `proposal` events whose inner kind is `axiom-mark`; removes on `commit` and `meta-disagreement-marked` events whose `payload.target === 'proposal'` (per ADR 0030 §9); returns surviving entries in proposal-arrival order.
- [`groupPendingAxiomMarksByNode(marks) → Map<string, PendingAxiomMark[]>` at line 303](../../../apps/moderator/src/graph/selectors.ts#L303) — buckets pending marks by their target node id for O(1) per-node enrichment lookups.

Line offsets are byte-identical to the `_v7` audit's snapshot — the only commit between `2debcc2` (`_v7` close) and this `_v8` audit is `2debcc2` itself, which touched only `tasks/27-shell-package.tji` and `tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v7.md`. Bodies are byte-identical. The header comment block immediately above the projector continues to document both the per-arrival ordering rule and the no-per-participant-uniqueness rule.

Callers (sole surface — moderator, unchanged since `_v7`):

- [`apps/moderator/src/graph/GraphCanvasPane.tsx`](../../../apps/moderator/src/graph/GraphCanvasPane.tsx) — imports `EMPTY_PENDING_AXIOM_MARKS`, `groupPendingAxiomMarksByNode`, and `projectPendingAxiomMarks`; runs the projector to produce the per-node fallback the moderator's graph canvas reads.
- [`apps/moderator/src/graph/StatementNode.tsx`](../../../apps/moderator/src/graph/StatementNode.tsx) — imports the `<PendingAxiomMarkBadge>` component and a type-only `PendingAxiomMark` import; consumes on `pendingAxiomMarks` prop downstream.
- [`apps/moderator/src/graph/PendingAxiomMarkBadge.tsx`](../../../apps/moderator/src/graph/PendingAxiomMarkBadge.tsx) — type-only `PendingAxiomMark` import — the dashed-faded badge component that paints each pending mark.

Test coverage: [`apps/moderator/src/graph/selectors.test.ts`](../../../apps/moderator/src/graph/selectors.test.ts) `describe('projectPendingAxiomMarks')` + `describe('groupPendingAxiomMarksByNode')` blocks (coverage unchanged from v2/v3/v4/v5/v6/v7 audit-time snapshots).

#### Participant surface — no pending-axiom-mark code at this commit; no scheduled work

[`apps/participant/src/`](../../../apps/participant/src/) at `_v8` refinement time: a recursive grep for `pendingAxiomMark` / `PendingAxiomMark` / `pending-axiom-mark` / `projectPendingAxiomMarks` / `EMPTY_PENDING_AXIOM_MARKS` returns **zero matches** — bit-for-bit identical to the `_v7` finding. No file in `apps/participant/src/graph/` or `apps/participant/src/proposals/` consumes the projector; no `<PendingAxiomMarkOverlay>` analog exists; no `PendingAxiomMarkBadge`-equivalent is rendered. The `part_axiom_mark_from_tablet` subtree at [`tasks/40-participant-ui.tji:208-245`](../../40-participant-ui.tji#L208) is `complete 100` for committed-axiom-mark workflow only; its deferred-∞ sub-tasks (`part_mark_axiom_action`, `part_axiom_mark_proposal`) name pending-mark authoring as future work but not pending-mark rendering. The `part_pending_proposals` task at [`tasks/40-participant-ui.tji:210`](../../40-participant-ui.tji#L210) shipped a list-view pane — not a graph-canvas pending-axiom-mark overlay or any consumer of `projectPendingAxiomMarks`. No follow-up task identifier in `tasks/40-participant-ui.tji` matches `part_axiom_mark_pending` / `part_pending_axiom_mark` / `pending_axiom_mark`. Precondition (a): unmet, unchanged from `_v7`.

#### Audience surface — no pending-axiom-mark code; no scheduled work

[`apps/audience/src/`](../../../apps/audience/src/) at `_v8` refinement time: a recursive grep for `pendingAxiomMark` / `PendingAxiomMark` / `pending-axiom-mark` returns **zero matches** — bit-for-bit identical to the `_v7` finding. No file in `apps/audience/src/graph/` consumes the projector; no `<AudiencePendingAxiomMarkOverlay>` analog exists. A grep of [`tasks/50-audience-and-broadcast.tji`](../../50-audience-and-broadcast.tji) for `aud_axiom_mark_pending` / `aud_pending_axiom_mark` / `pending_axiom_mark` returns **zero matches**. The deferred leaves under `audience.aud_graph_rendering` (`aud_pending_proposals_pane`, `aud_pending_proposals_pending_votes`, `aud_annotation_rendering`, `aud_annotation_rendering_edges`) scope a pending-proposals pane and annotation rendering but NOT pending-axiom-mark graph-canvas rendering. The future polish leaf named in prose ([`aud_axiom_mark_decoration.md:38`](../audience/aud_axiom_mark_decoration.md#L38) — `aud_axiom_mark_pending_render`, ~1d) remains explicitly NOT registered today. Precondition (b): unmet, unchanged from `_v7`.

#### Existing shell substrate — `axiom-marks/` sibling directory exists; `pending-axiom-marks/` does not

[`packages/shell/src/`](../../../packages/shell/src/) at `_v8` refinement time hosts the existing substrate directories — `annotations/`, `auth/`, `axiom-marks/` (committed; lifted by `shell_axiom_marks_extraction`), `error-mapper/`, `facet-pill/`, `facet-status/`, `i18n/`, `login-logout/`, `mount-contract/`, `screen-name/`, `slots/`, `votes-by-facet/`, `ws/`, etc. **No `pending-axiom-marks/` directory exists.** A grep of [`packages/shell/src/`](../../../packages/shell/src/) for `PendingAxiomMark` returns only an incidental comment reference at [`facet-pill/participant-color.ts:2`](../../../packages/shell/src/facet-pill/participant-color.ts#L2) (a doc-line naming `<PendingAxiomMarkBadge>` as a downstream consumer of the deterministic `axiomMarkColorFor` palette — not an active import; same allowed comment-only reference as `_v2` → `_v7`). The hosting infrastructure (sibling-directory layout + root re-export pattern + ready re-export block slot in `packages/shell/src/index.ts`) is ready for the future lift.

#### Cadence-rule pickup-signal state at refinement time

The `_v7` Decision §4 named two pickup signals for `_v8` (inherited from `_v6` Decision §3); both are observable at this refinement time:

- **Signal (i) — 4 or more orchestrator-pick passes elapsed since `_v7` close.** `_v7` closed at commit `2debcc2` on 2026-05-29. The orchestrator's pick-task pass surfaced `_v8` on the **first** pick after `_v7` closed. 1 pass < 4 passes; signal (i) has not fired.
- **Signal (ii) — a commit landed touching `apps/{participant,audience}/src/`, `tasks/{40-participant-ui,50-audience-and-broadcast}.tji`, or `tasks/refinements/{participant-ui/part_axiom_mark_decoration,audience/aud_axiom_mark_decoration}.md`, AND adding/removing/modifying a line matching the pending-axiom-mark identifier set (case-insensitive).** The only commit landed since `_v7` close (`2debcc2`) is `2debcc2` itself. `2debcc2` touched [`tasks/27-shell-package.tji`](../../27-shell-package.tji) and [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v7.md`](extract_pending_axiom_mark_projector_v7.md) — **neither in the precondition-bearing file list** (that list intentionally excludes shell-package's own files to prevent the chain's own close-commits from looping back as pickup signals). Signal (ii) has not fired.

Both signals unmet; the orchestrator's pick-task pass picked `_v8` anyway. The pickup-state is **bit-for-bit identical to `_v7`'s pickup-state one iteration earlier** — same signal-(i) count (1 pass), same signal-(ii) status (only the predecessor's own close-commit since the predecessor closed), same structural reason (prose-only cadence in `.tji` `note` blocks has no `tj3`-enforceable scheduling effect; the orchestrator's pick-task pass walks `complete 100` markers and `depends` edges, picking the next leaf whose `depends` edges are all `complete 100` regardless of `note`-block text). The two-iteration evidence window has closed in the affirmative direction: the prose-only-cadence-unhonored pattern is not iteration-specific noise but a structural property of the orchestrator's mechanical scheduler. Decision §3 acts on this confirmation.

## Constraints / requirements

- **Do not lift anything.** Both preconditions (a) and (b) named by the v1 audit's Decision §4 and re-confirmed by `_v2` → `_v7` Decisions §1 must be met before any shell directory is created or any caller is rewired. Both are unmet at this leaf's commit time, in bit-for-bit identical state to the `_v7` audit one orchestrator-pick pass earlier. The audit is part of the deliverable; the second-consecutive cadence-rule-violation observation (Decision §3) plus the escalation decision (Decision §3) plus the `_v9` + cadence-gate registration (Decision §4) are the rest. (Decision §1.)
- **Do not relax the lift trigger.** The asymmetry with the sibling `extract_votes_by_facet_projector_v2`'s relaxable trigger is unchanged at `_v8` audit time; the methodology is settled in the direction of zero additional callers; relaxing the trigger here would lift a one-caller projector on the strength of a methodology decision that excludes second + third callers — the worst-case YAGNI inversion. (Decision §1, alternative B; inherited from `_v2` → `_v7`.) The cadence-enforcement escalation in Decision §3 does NOT change the lift trigger itself; it changes only the enforcement of the pickup-cadence (which gates WHEN `_v9` is picked, not WHETHER the lift fires).
- **Do not introduce a merged-axiom-mark projector** (pending + committed in one shell entry). Already rejected by v1 Decision §1 alternative C; inherited verbatim through `_v2` → `_v7` to here. (Decision §1.)
- **Do not pre-register the participant or audience pending-axiom-mark leaves from this shell-package refinement.** Both surfaces have explicit refinement-stage decisions deferring those leaves until a concrete UX trigger arrives. Pre-registering them here inverts the WBS's dependency direction. (Decision §2, inheriting v1 → v7 Decision §2.)
- **Do not edit the four moderator-local symbols or their three consumers.** No re-comment, no signature change, no `Readonly` widen, no extraction of an adapter. The symbols stay at [`apps/moderator/src/graph/selectors.ts`](../../../apps/moderator/src/graph/selectors.ts) verbatim; the three moderator consumers (`GraphCanvasPane.tsx`, `StatementNode.tsx`, `PendingAxiomMarkBadge.tsx`) stay byte-identical. Their existing Vitest suite stays in place.
- **Do not edit any `.tji` file.** Per the refinement-writer brief, the orchestrator / closer own WBS shape. Decision §4 names BOTH follow-up tasks (`_v9` AND `_v9_cadence_gate`) crisply — effort + one-line description + the gate's `complete 0` status + the flip-protocol — so the closer can register them mechanically.
- **Do not add an ADR.** The methodology question this audit re-raises (are pending-axiom-marks a moderator-only proposal-author affordance, or do they belong on participant/audience surfaces too?) has not changed since v1 → v7; one further audit iteration's confirmation does not warrant an ADR. The cadence-gate task structure introduced by Decision §3 is a WBS-cadence policy applied to one audit chain, not an architectural decision affecting the codebase. If two or more audit chains adopt the same gate structure (cross-chain reuse), or if a future refinement folds the gate into a meta-policy mechanism (e.g., a shared `cadence_gate` task convention), THAT work's refinement may amend the relevant ADR or open a new one; this audit does not pre-abstract.
- **Do act on the cadence-enforcement escalation at `_v8`.** The `_v7` Decision §4 cadence-enforcement-revisit clause fires on a second-consecutive cadence violation. This `_v8` pickup is exactly that second-consecutive violation. The v3 precedent's two-iteration evidence-window discipline demands action at the second observation; "hold prose-only one more iteration" violates that discipline and is rejected as Decision §3 alternative C.
- **Do not choose the "non-leaf watcher" escalation path (path (2) from `_v7` Decision §3).** Decision §3 rejects this path: it requires amending the orchestrator's pick-task workflow (meta-policy work beyond this audit chain's scope) and introduces an abstraction whose value is amortized only across multiple chains adopting similar policies — neither of which is justified by this single-chain cadence-violation pattern. The `tj3`-enforceable cadence-gate task (path (1)) stays entirely inside `tj3`'s pick-task pass semantics and is the right rate of escalation for this iteration.
- **Do not bind the cadence-gate's flip to a wall-clock calendar date.** Already rejected by `_v5` Decision §3 alternative D, `_v6` Decision §3 alternative D, and `_v7` (per its inheritance); inherited verbatim. The gate's flip-protocol is signal-based (signal (i): elapsed-pick-pass count; signal (ii): precondition-touching commit), not calendar-based.
- **No Playwright / Cucumber coverage added.** Pure documentation deliverable; no protocol seam crossed, no projector output changed, no user-visible behavior shift. The UI-stream e2e policy in the refinement-writer brief does not apply (this is not a UI-stream leaf and creates no new component or event surface). The backend / projector / methodology-engine guidance does not apply (projector output and wire format unchanged — no Cucumber pin warranted).

## Acceptance criteria

- [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v8.md`](extract_pending_axiom_mark_projector_v8.md) (this file) lands in the same commit as the `complete 100` marker on `shell_package.extract_pending_axiom_mark_projector_v8` in [`tasks/27-shell-package.tji`](../../27-shell-package.tji). The file contains: the audit of both preconditions, the structural reason both remain unmet AND principledly absent, the unchanged asymmetry with [`extract_votes_by_facet_projector_v2`](extract_votes_by_facet_projector_v2.md)'s relaxable trigger, the second-consecutive cadence-rule-violation observation with its structural explanation (Decision §3), the escalation decision (Decision §3 chooses path (1) — a `tj3`-enforceable cadence-gate task — over path (2) and over "hold prose-only"), and the crisp registration of TWO follow-up tasks that Decision §4 names (`_v9` AND `_v9_cadence_gate`).
- **No new code in `packages/shell/src/`.** A `grep` of [`packages/shell/src/`](../../../packages/shell/src/) for active (non-comment) references to `PendingAxiomMark` / `projectPendingAxiomMarks` / `groupPendingAxiomMarksByNode` / `EMPTY_PENDING_AXIOM_MARKS` / `pending-axiom-marks` returns zero results. The incidental comment reference at [`facet-pill/participant-color.ts:2`](../../../packages/shell/src/facet-pill/participant-color.ts#L2) is allowed (it documents downstream component naming, not active code).
- **No edits to the moderator-local symbols.** `git diff HEAD` on [`apps/moderator/src/graph/selectors.ts`](../../../apps/moderator/src/graph/selectors.ts) is empty for this commit; the projector body, the grouper body, the `PendingAxiomMark` interface, and the `EMPTY_PENDING_AXIOM_MARKS` constant stay byte-identical.
- **No edits to the moderator's consumers.** `git diff HEAD` on [`apps/moderator/src/graph/GraphCanvasPane.tsx`](../../../apps/moderator/src/graph/GraphCanvasPane.tsx), [`apps/moderator/src/graph/StatementNode.tsx`](../../../apps/moderator/src/graph/StatementNode.tsx), and [`apps/moderator/src/graph/PendingAxiomMarkBadge.tsx`](../../../apps/moderator/src/graph/PendingAxiomMarkBadge.tsx) is empty.
- **No edits to `packages/shell/src/index.ts`.** No new pending-axiom-marks re-export block.
- **TWO follow-up named-future-tasks are crisply named in Decision §4**, ready for the closer to register mechanically in [`tasks/27-shell-package.tji`](../../27-shell-package.tji):
  1. `shell_package.extract_pending_axiom_mark_projector_v9` (effort 0.5d; the audit-iteration leaf, `depends` on both `!extract_pending_axiom_mark_projector_v8` AND `!extract_pending_axiom_mark_projector_v9_cadence_gate`); deferred to: `shell_package.extract_pending_axiom_mark_projector_v9` (closer registers in WBS).
  2. `shell_package.extract_pending_axiom_mark_projector_v9_cadence_gate` (effort 0.1d; a milestone-style sentinel task, `complete 0` at registration time, flipped to `complete 100` only when a closer or orchestrator deliberately verifies that one of the cadence signals (i) or (ii) has fired — full flip-protocol in Decision §4); deferred to: `shell_package.extract_pending_axiom_mark_projector_v9_cadence_gate` (closer registers in WBS).
- **No new participant or audience leaves are scoped from this refinement.** Decision §2 inherits v1 → v7's explicit decision to leave materialization of those surfaces' pending-axiom-mark callers to their own work-streams. The future `_v9` task's lift trigger references the unchanged "both UI leaves `complete 100`" condition; its pickup is gated by the new `_v9_cadence_gate` sentinel (Decision §3); neither names specific candidate participant or audience leaves.
- **Cadence-enforcement escalation acted on at this iteration.** Decision §3 chooses path (1) (the `tj3`-enforceable cadence-gate task) from `_v7` Decision §3's two pre-named escalation paths; path (2) (the "non-leaf watcher" pattern) is rejected as out-of-scope meta-policy work; the third alternative ("hold prose-only") is rejected as violating the v3 precedent's two-iteration evidence-window discipline. The escalation is implemented via the `_v9_cadence_gate` registration in Decision §4 (the closer's two-task handoff).
- **TaskJuggler validity preserved.** `tj3 project.tjp 2>&1 | grep -iE "error|fatal"` returns silent after the closer's `complete 100` + the two-task `_v9` + `_v9_cadence_gate` registration (the pre-commit hook enforces this per [`docs/dev-environment.md`](../../../docs/dev-environment.md)).
- **No throwaway verification scripts.** Per ADR 0022, this audit's deliverable is the refinement document itself; the moderator's existing Vitest suite ([`apps/moderator/src/graph/selectors.test.ts`](../../../apps/moderator/src/graph/selectors.test.ts) `projectPendingAxiomMarks` + `groupPendingAxiomMarksByNode` describe blocks) continues to pin the moderator surface's local behavior unchanged. No new test, no smoke script, no executable artifact.
- **No Playwright / Cucumber scoped.** Pure documentation; no behavior change at any seam. The UI-stream e2e policy does not apply (not a UI-stream leaf; no new component / event surface). The projector / methodology-engine guidance does not apply (projector output unchanged).
- **Status block records audit outcome AND the second-consecutive cadence-rule-violation observation AND the escalation decision AND the two-task `_v9` re-registration.** When the closer appends the `## Status` block, it records: "audit complete; both preconditions still unmet, bit-for-bit identical to `_v7` findings one orchestrator-pick pass earlier; cadence-rule pickup signals (i) and (ii) both unmet at pick time for the second consecutive iteration, pattern confirmed across the two-iteration evidence window the `_v3` precedent's discipline named; cadence-enforcement-revisit clause inherited from `_v7` Decision §4 fires; escalation chosen — `tj3`-enforceable cadence-gate task per `_v7` Decision §3 path (1) (Decision §3); lift deferred to `shell_package.extract_pending_axiom_mark_projector_v9`, gated by new `shell_package.extract_pending_axiom_mark_projector_v9_cadence_gate` sentinel (Decision §4)." Prior sections (Decisions, Inputs/context) stay untouched per the [`tasks/refinements/README.md`](../README.md) ritual.

## Decisions

### §1 — Both preconditions remain unmet AND principledly absent; defer again (do NOT relax the lift trigger on the sibling `_v2` lift's precedent)

The predecessor `_v7` audit ([`extract_pending_axiom_mark_projector_v7.md`](extract_pending_axiom_mark_projector_v7.md) Decision §4) registered THIS `_v8` task with the trigger *"fires when **both** … (a) a participant pending-axiom-mark rendering leaf … is `complete 100`, AND (b) an audience pending-axiom-mark rendering leaf … is `complete 100`,"* identical to v1 → v6's registrations of `_v2` → `_v7`. The audit at refinement time finds neither leaf exists nor is scheduled, and **the principled-absence the v1 → v7 audits all recorded has not been revised at either surface**:

- **Participant surface — still committed-only.** [`part_axiom_mark_decoration.md:27`](../participant-ui/part_axiom_mark_decoration.md#L27) stance is unchanged; zero participant code consumes the projector; zero task identifier in [`tasks/40-participant-ui.tji`](../../40-participant-ui.tji) names a future participant pending-axiom-mark rendering leaf. The refinement's Status line ("Done — 2026-05-17") has no amendment. The `part_axiom_mark_from_tablet` subtree (`complete 100`, committed-only) and the `part_pending_proposals` task (shipped a list-view pane) are both scope-disjoint from pending-axiom-mark **graph-canvas** rendering.
- **Audience surface — still committed-only.** [`aud_axiom_mark_decoration.md:38`](../audience/aud_axiom_mark_decoration.md#L38) stance is unchanged; zero audience code consumes the projector; the prose-named future polish leaf (`aud_axiom_mark_pending_render`, ~1d) remains explicitly NOT pre-registered. The refinement's Status line ("Done — 2026-05-28") has no amendment. The `audience.aud_graph_rendering` subtree (30/52 `complete 100`) has no leaf scoping pending-axiom-mark graph-canvas rendering.

The asymmetry with [`extract_votes_by_facet_projector_v2.md`](extract_votes_by_facet_projector_v2.md) (settled 2026-05-28 at commit `8c7087b`) is unchanged from `_v2` → `_v7`'s recording:

- The votes-by-facet projector had two callers with a methodology-shaped divergence. Resolving the methodology produced two existing callers in lockstep. The "third workspace" criterion was the weak link; relaxing it cost nothing because the strength-of-evidence was already at two byte-identical callers.
- The pending-axiom-marks projector has ONE caller — by design. The participant + audience surfaces have explicitly decided not to render pending-axiom-marks. There is no in-flight methodology task; the methodology has already concluded that pending-axiom-marks belong at the proposal-author surface only. Relaxing the trigger here means lifting a one-caller projector into shell on the strength of a methodology decision that says no second caller exists.

Three options for what to do this turn (on the lift trigger; the cadence-enforcement escalation is Decision §3):

- **(A — chosen)** Defer again. Mark this `_v8` leaf `complete 100`; register `_v9` with identical lift-trigger semantics (both UI leaves `complete 100`). The lift continues to wait for both UI surfaces to revise their principled-absence decisions and ship concrete rendering. Reasoning: the trigger is binary on UI work that is not scheduled and is not in-flight; deferring against an unscheduled trigger is the right call exactly because the alternative (lifting on the strength of a methodology decision that excludes the additional callers) is worst-case YAGNI inversion. The audit is part of the deliverable for this iteration; the escalation decision (Decision §3) and the two-task re-registration (Decision §4) are the rest; the lift is the deliverable when (and if) the principled-absence reverses.
- **(B)** Relax the lift trigger on the votes-by-facet precedent — lift now, with one workspace caller (moderator). Cost: introduces a single-caller shell entry with one workspace user — the anti-pattern the v1 audit Decision §1 alternative B already rejected; the sibling `_v2`'s relaxation is not a precedent for THIS lift because the structural conditions are inverted. Rejected (same reasoning as v1 → v7 Decision §1 alternative B).
- **(C)** Defer indefinitely AND drop the `_v9` registration. Cost: loses the citable audit chain; the orchestrator's tech-debt registration policy asks for a crisp future-task; dropping the registration violates that policy. Future implementers re-discovering the deferral from cold would have to re-derive the recipe + the rejection of relaxation alternatives without the audit chain to reference. Rejected (same reasoning as v1 → v7 Decision §1 alternative C). The escalation in Decision §3 + the two-task re-registration in Decision §4 address the cadence-violation concern that another reader might motivate this alternative with.

Chosen: (A). The three-caller policy floor is three; the current caller count is one; the methodology's authority for additional callers is exclusionary, not enabling. The natural path is to continue waiting until the floor is reached by a genuine revision at the UI work-streams, then lift via the same recipe `extract_cytoscape_projectors`, `shell_axiom_marks_extraction`, and `extract_votes_by_facet_projector_v2` followed.

### §2 — Do not pre-register participant / audience pending-axiom-mark leaves from this shell-package refinement (inherited from v1 → v7 Decision §2; restated for the `_v8` audit cadence)

The v1 → v7 audits' Decision §2 rejected pre-registering `part_pending_proposals.*` graph-canvas overlay leaves or the audience `aud_axiom_mark_pending_render` leaf from a shell-package refinement, on the grounds that shell-package refinements depend on UI surfaces materializing callers (not the other way around) and that the audience's own refinement explicitly framed its future polish leaf as "registered only when a concrete trigger materializes." The same reasoning applies one further audit-iteration later; both surfaces' explicit decline-to-register decisions remain unrevised.

Three options:

- **(A — chosen)** Do NOT pre-register `part_pending_proposals.*` graph-canvas additions or `aud_axiom_mark_pending_render` from this refinement. The future `_v9` task's lift trigger references the unchanged "both UI leaves `complete 100`" condition; the specific candidate leaves are named by the participant + audience work-streams when (and if) they reach the source-of-debt stage. Reasoning: inherits v1 → v7 Decision §2 verbatim; the principled-absence is durable across eight audit iterations, so re-asking the question at the new gated cadence is the right rate — not pre-registering them.
- **(B)** Pre-register one or both leaves now to anchor the future lift's `depends` edges. Cost: contradicts both predecessor UI refinements' explicit decline-to-register decisions; shell-package refinement scopes UI work; introduces speculative leaves on the WBS that may never fire. Rejected (same reasoning as v1 → v7 Decision §2 alternative B).
- **(C)** Pre-register a single combined cross-surface leaf bundling participant + audience pending-axiom-mark rendering as one ~2d ticket. Cost: tangles two surfaces' independently-arrived-at design decisions; bundling produces a leaf neither work-stream owns. Rejected (same reasoning as v1 → v7 Decision §2 alternative C).

Chosen: (A). The future `_v9` task's trigger reads the actual condition ("both UI leaves `complete 100`"); the path to satisfaction is held by the audience and participant work-streams' own future design.

### §3 — Second-consecutive cadence-rule-violation observed; the two-iteration evidence window closes in the affirmative direction; choose escalation path (1) — a `tj3`-enforceable cadence-gate task

The `_v7` Decision §4 cadence-enforcement-revisit clause specified: *"if `_v8`'s pickup also violates the cadence (pick-pass-1 or pick-pass-2 pickup with signal (ii) unmet — i.e., a second consecutive cadence violation), `_v8`'s refinement re-evaluates whether to escalate the cadence enforcement mechanism per the two paths named in Decision §3."* The orchestrator's pick-task pass surfaced `_v8` on the **first** pick after `_v7` closed, with signal (ii) also unfired — a state in which:

- **Signal (i) — unmet.** 1 pass < 4 passes. The pickup horizon `_v7` Decision §4 named has not been reached.
- **Signal (ii) — unmet.** The only commit since `_v7` close is `2debcc2` itself, which touched [`tasks/27-shell-package.tji`](../../27-shell-package.tji) and [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v7.md`](extract_pending_axiom_mark_projector_v7.md). Neither file is in the precondition-bearing list the cadence-(ii) signal scopes — that list intentionally excludes shell-package's own files to prevent the chain's own close-commits from looping back as pickup signals.

This is **bit-for-bit identical to the `_v7` pickup-state observation** (same signal-(i) count, same signal-(ii) status, same structural reason). The two-iteration evidence window the `_v3` precedent's discipline named has closed in the affirmative direction: prose-only cadence in `.tji` `note` blocks has no `tj3`-enforceable scheduling effect; the orchestrator's pick-task pass walks `complete 100` markers and `depends` edges, picking the next leaf whose `depends` edges are all `complete 100` regardless of `note`-block text. This is not iteration-specific noise — it is a structural property of the orchestrator's mechanical scheduler, confirmed across two consecutive iterations exactly per the precedent's evidence-gathering rate.

The cadence-enforcement-revisit clause therefore **fires**, and the v3 precedent's discipline **demands action** at this iteration. The two-iteration window was designed to gather exactly this evidence; holding prose-only one more iteration would violate the precedent's evidence-gathering rate by over-collecting evidence past the threshold the rule itself set.

Four options for the cadence-enforcement escalation:

- **(A — chosen)** Choose `_v7` Decision §3's path (1): introduce a `tj3`-enforceable cadence-gate task. Decision §4 of THIS refinement registers `shell_package.extract_pending_axiom_mark_projector_v9_cadence_gate` as a sibling milestone-style sentinel task, `complete 0` at registration time, with `effort 0.1d` and a flip-protocol bound to signal (i) or signal (ii) firing (NOT calendar dates — same signal-based mechanism as `_v6`/`_v7`/`_v8`'s prose cadence, now enforced structurally). `_v9` is registered with `depends ! extract_pending_axiom_mark_projector_v8 + ! extract_pending_axiom_mark_projector_v9_cadence_gate` — so the orchestrator's pick-task pass cannot surface `_v9` until BOTH `_v8` is `complete 100` (the chain's continuity edge) AND the gate is `complete 100` (the cadence enforcement edge). The gate's flip is a deliberate human action by the closer or by an orchestrator review pass, performed only when one of the signals has been verified to fire. **This is the orchestrator brief's "formal blocking dependency added to the WBS" enforcement mechanism, applied concretely.** Reasoning: keeps the enforcement entirely inside `tj3`'s pick-task pass semantics (the orchestrator's pick-task pass walks `depends` edges, so a `complete 0` gate blocks the successor mechanically); is single-chain in scope (the gate is named per-chain — `_v9_cadence_gate` — so other chains adopting the same pattern do so independently); is reversible at low cost (if the gate's flip-protocol turns out to be too aggressive or too lax at `_v9`/`_v10`, the `_vN+1` refinement can re-scope the signals).
- **(B)** Choose `_v7` Decision §3's path (2): introduce a non-leaf "watcher" pattern requiring a brief amendment to the orchestrator's pick-task workflow. Cost: requires amending the orchestrator's pick-task workflow (meta-policy work beyond this audit chain's scope); introduces an abstraction whose value is amortized only across multiple chains adopting similar policies (no other chain has reached the second-consecutive-violation threshold yet); the watcher pattern's value as a shared mechanism only emerges if multiple chains adopt it, which this audit cannot pre-justify. Rejected.
- **(C)** "Hold prose-only" — carry the same batched cadence to `_v9` with the same prose-only enforcement, accepting that the orchestrator's pick-task pass will continue picking every successor-of-closed-leaf on the first pass. Cost: violates the v3 precedent's two-iteration evidence-window discipline (the window has closed; holding prose-only past the threshold is over-collecting evidence); renders the cadence-enforcement-revisit clause vestigial (the clause was specifically designed to act on confirmed evidence, and the evidence is confirmed); produces a third iteration of pickup-at-pick-pass-1, perpetuating the per-pass churn the cadence policy was designed to prevent. Rejected.
- **(D)** Implement BOTH path (1) AND path (2) at `_v9` — register the cadence-gate sentinel AND propose an orchestrator pick-task workflow amendment. Cost: doubles the implementation work without doubling the value (path (1) alone resolves the cadence-rule-violation pattern for this chain; path (2) is amortization-pending across multiple chains); mixes single-chain and meta-policy escalation in the same iteration; the orchestrator workflow amendment is the kind of work the meta-policy layer owns, not this audit. Rejected.

Chosen: (A). The `tj3`-enforceable cadence-gate task is the right rate of escalation for the second-consecutive-violation observation: it keeps the enforcement inside `tj3` (where the orchestrator's pick-task pass actually consults), it is single-chain in scope (no premature meta-abstraction), and it is reversible at low cost if the flip-protocol needs tuning at `_v10` or later. The gate's flip-protocol is named explicitly in Decision §4 below.

Note on what the cadence-enforcement escalation does NOT change:

- The **lift trigger** is unchanged — `_v9` still fires the lift only when both UI leaves are `complete 100`. The gate gates WHEN `_v9` is picked (the pickup cadence), not WHETHER the lift fires (the lift trigger).
- The **rejection of relaxation alternatives** is unchanged — the same v1-Decision-§1 reasoning continues to govern.
- The **shell directory layout, the symbol list, and the recipe** are unchanged — when the lift fires, it follows `shell_axiom_marks_extraction` + `extract_cytoscape_projectors` + `extract_votes_by_facet_projector_v2` verbatim.
- The **cadence rate** is unchanged — `_v9` adopts the same signal-based cadence the `_v6`/`_v7`/`_v8` chain has used (signal (i): ≥4 elapsed pick-passes; signal (ii): a precondition-touching commit). Only the enforcement mechanism changes from prose-only to a `tj3`-enforceable gate.

Note on the gate's flip-protocol design:

- The gate's `complete 100` is gated by **deliberate human verification** of one of the cadence signals firing. The verifier (closer or orchestrator review pass) reads the gate's `note` block, runs the signal-(i) elapsed-pick-pass count and the signal-(ii) `git log`/`git show` precondition-touching-commit check, and flips the gate to `complete 100` ONLY when at least one signal has fired. The flip itself is a single-line `complete 100` edit to [`tasks/27-shell-package.tji`](../../27-shell-package.tji).
- The gate is NOT auto-flipped by the orchestrator's pick-task pass (that would re-introduce the prose-vs-`tj3`-enforceable gap at a higher level of indirection). The verifier-flips-the-gate design is the structural enforcement: the orchestrator's pick-task pass walks `depends` edges and finds the gate `complete 0`, so it mechanically skips `_v9` until a human (or human-driven orchestrator pass) deliberately flips the gate after verifying signals.
- The gate's `note` block names BOTH signals identically to the `_v6`/`_v7`/`_v8` cadence text, so a verifier reading the gate at flip-time has the full criteria in front of them. The gate's full text is in Decision §4 below.

### §4 — Register two follow-up named-future-tasks: `shell_package.extract_pending_axiom_mark_projector_v9` (gated) AND `shell_package.extract_pending_axiom_mark_projector_v9_cadence_gate` (sentinel)

The audit identifies one deferred workstream (the shell-extract lift) waiting on the same binary lift trigger as v1 → `_v7`: both UI leaves `complete 100`. The escalation chosen in Decision §3 implements that lift's cadence-enforcement via a `tj3`-enforceable gate sentinel sibling. Two named-future-tasks together capture the lift + the enforcement:

**Task 1 (shell-package workstream, deferred — the audit-iteration leaf):**

- **Name**: `shell_package.extract_pending_axiom_mark_projector_v9`
- **Effort**: 0.5d (the lift itself: create `packages/shell/src/pending-axiom-marks/`, lift `PendingAxiomMark` + `EMPTY_PENDING_AXIOM_MARKS` + `projectPendingAxiomMarks` + `groupPendingAxiomMarksByNode` + the consolidated Vitest suite, rewire all three or more callers' imports; same shape `shell_axiom_marks_extraction`, `extract_cytoscape_projectors`, and `extract_votes_by_facet_projector_v2` followed for their respective vocabularies). 0.5d holds for the lift because the body is unchanged; if the trigger has not fired by `_v9`'s pickup (i.e., the gate is flipped on signal (i) sweep ceiling rather than on a precondition-touching commit), the audit deliverable consumes ~0.25d and the remaining budget covers whatever follow-up cadence-policy decision the next iteration's evidence justifies.
- **Description**: "Lift `projectPendingAxiomMarks` + `groupPendingAxiomMarksByNode` + `PendingAxiomMark` + `EMPTY_PENDING_AXIOM_MARKS` from `apps/moderator/src/graph/selectors.ts` into `packages/shell/src/pending-axiom-marks/` once BOTH a participant pending-axiom-mark rendering leaf AND an audience pending-axiom-mark rendering leaf land (three-caller convergence). The participant and audience surfaces have explicitly declined to render pending-axiom-marks ([`part_axiom_mark_decoration.md:27`](../participant-ui/part_axiom_mark_decoration.md#L27), [`aud_axiom_mark_decoration.md:38`](../audience/aud_axiom_mark_decoration.md#L38)); this lift fires only after both surfaces revise that decision and ship concrete rendering. Eight audit iterations (v1, v2, v3, v4, v5, v6, v7, v8 — all settled 2026-05-28 → 2026-05-29) confirmed the trigger remains unmet AND principledly absent at each cadence. `_v7` and `_v8` each observed that the prose-only batched cadence the `_v6` closer scoped was unhonored at pick time; the second-consecutive observation closed the v3 precedent's two-iteration evidence window and `_v8` Decision §3 escalated to a `tj3`-enforceable cadence-gate task. `_v9`'s pickup is now gated by the `_v9_cadence_gate` sentinel — the orchestrator's pick-task pass cannot surface `_v9` until BOTH `_v8` is `complete 100` AND the gate is `complete 100` (flipped by a closer or orchestrator review pass only after verifying one of the cadence signals has fired)."
- **Home**: [`tasks/27-shell-package.tji`](../../27-shell-package.tji) (shell-package surface).
- **Lift trigger** (unchanged from v1 → v8): fires when **both** of the following hold: (a) a participant pending-axiom-mark rendering leaf (likely under a `part_pending_proposals.*` group, per [`part_axiom_mark_decoration.md:27`](../participant-ui/part_axiom_mark_decoration.md#L27)) is `complete 100`, AND (b) an audience pending-axiom-mark rendering leaf (likely `aud_axiom_mark_pending_render`, per [`aud_axiom_mark_decoration.md:38`](../audience/aud_axiom_mark_decoration.md#L38)) is `complete 100`. Both leaves must verbatim-port the moderator's projector (or a documented superset thereof); the three-caller convergence check is what makes the lift safe.
- **Depends on** (TWO `!`-edges; both required `complete 100` for orchestrator pick-task pass to surface `_v9`):
  1. `! shell_package.extract_pending_axiom_mark_projector_v8` (the chain's continuity edge — `_v8` `complete 100` after this refinement).
  2. `! shell_package.extract_pending_axiom_mark_projector_v9_cadence_gate` (the cadence enforcement edge — gate `complete 0` at registration, flipped per the flip-protocol named in Task 2 below).
  The two future UI leaves' edges are added when those leaves materialize; the future-task's dependency declaration is updated at that point.
- **Source of debt**: this refinement, Decisions §1-§3.

**Task 2 (shell-package workstream, registration-time `complete 0` — the cadence enforcement sentinel):**

- **Name**: `shell_package.extract_pending_axiom_mark_projector_v9_cadence_gate`
- **Effort**: 0.1d (the verify-and-flip work: the closer or orchestrator review pass reads the gate's `note` block, runs the signal-(i) elapsed-pick-pass count and the signal-(ii) `git log`/`git show` precondition-touching-commit check, and flips the gate to `complete 100` ONLY when at least one signal has fired. The flip itself is a single-line edit to [`tasks/27-shell-package.tji`](../../27-shell-package.tji); the verification is the substantive portion of the budget).
- **Description**: "Cadence-enforcement gate sentinel for the `extract_pending_axiom_mark_projector` audit-iteration chain. Closer or orchestrator review pass flips this gate to `complete 100` ONLY after deliberately verifying that one of the following signals has fired: (i) 4 or more orchestrator-pick passes have elapsed since `_v8` close, OR (ii) a commit has landed touching one of `apps/{participant,audience}/src/`, `tasks/{40-participant-ui,50-audience-and-broadcast}.tji`, or `tasks/refinements/{participant-ui/part_axiom_mark_decoration,audience/aud_axiom_mark_decoration}.md` AND adding/removing/modifying a line matching (case-insensitive) `pendingAxiomMark|PendingAxiomMark|pending-axiom-mark|pending_axiom_mark|axiom_mark_pending|axiom_pending`. Until one signal fires, this gate stays `complete 0` and the orchestrator's pick-task pass mechanically skips `_v9`. The gate is NOT auto-flipped — the verifier is a human (closer or orchestrator review pass) who runs the signal checks deliberately. Source: `_v8` refinement Decision §3 (escalation path (1) — `tj3`-enforceable cadence-gate task — chosen after the two-iteration evidence window confirmed the prose-only-cadence-unhonored pattern across `_v7` and `_v8`)."
- **Home**: [`tasks/27-shell-package.tji`](../../27-shell-package.tji) (shell-package surface; sibling of `_v9`).
- **Initial `complete` value**: `complete 0` at registration time.
- **Flip-protocol** (must be present in the gate's `note` block verbatim so the verifier has the criteria in front of them):
  1. **Signal (i) check**: run `git log --oneline --all <_v8-close-commit-hash>..HEAD | wc -l` and confirm ≥ 4 orchestrator-pick-pass-equivalent commits have landed since `_v8` close. (Pick-pass-equivalent is approximated as one closure commit; the verifier may refine this count by examining the orchestrator's pick-task pass log if available.)
  2. **Signal (ii) check**: run `git log <_v8-close-commit-hash>..HEAD --diff-filter=ACDMR -- apps/participant/src/ apps/audience/src/ tasks/40-participant-ui.tji tasks/50-audience-and-broadcast.tji tasks/refinements/participant-ui/part_axiom_mark_decoration.md tasks/refinements/audience/aud_axiom_mark_decoration.md` and confirm at least one commit's diff contains a case-insensitive match for `pendingAxiomMark|PendingAxiomMark|pending-axiom-mark|pending_axiom_mark|axiom_mark_pending|axiom_pending` (added, removed, or modified line).
  3. If **either** signal fires, flip the gate to `complete 100` with a one-line commit message naming which signal fired. If **neither** fires, the gate stays `complete 0` and the orchestrator's pick-task pass continues to skip `_v9`. The verifier MUST NOT flip the gate on a calendar-date basis or on any signal other than (i) or (ii) — calendar-date binding was rejected by `_v5`/`_v6`/`_v7` Decision §3 alternative D and remains rejected here.
- **Depends on**: nothing (the gate is unconditioned in the WBS graph; its `complete 100` is gated purely by human-verified signal firing). The gate may be registered alongside `_v9` in the same closer commit landing this `_v8` audit.
- **Source of debt**: this refinement, Decision §3 (escalation path (1) — `tj3`-enforceable cadence-gate task — chosen after the two-iteration evidence window confirmed the prose-only-cadence-unhonored pattern across `_v7` and `_v8`).

Two reasons for the two-task structure (rather than a single `_v9` with embedded prose-only cadence):

- The two-task split is what makes the cadence enforcement structural. A single `_v9` with prose-only cadence reproduces the `_v7`/`_v8` pattern exactly (the orchestrator's pick-task pass walks `complete 100` + `depends` edges and ignores `note`-block text); the gate's `depends`-edge contribution is the mechanism that forces the orchestrator's pick-task pass to skip `_v9` until human verification fires.
- The gate's separability lets future iterations adjust the flip-protocol (e.g., loosen signal (ii) to include additional file paths, or tighten signal (i) to a different pick-pass count) without amending `_v9`'s own description. Each iteration may close `_vN_cadence_gate` and the iteration's own `_vN` `depends`-edge points at the next gate (e.g., `_v10_cadence_gate`). The audit chain stays clean; the cadence policy evolves in its own sentinel-task lineage.

Three rejected alternatives (for the two-task structure):

- **One combined task** (`_v9` only, with a prose-only cadence note). Cost: reproduces the `_v7`/`_v8` cadence-rule-violation pattern exactly; the prose-only cadence has no `tj3`-enforceable effect; the escalation chosen in Decision §3 would not be implemented. Rejected.
- **Three tasks** (`_v9`, `_v9_cadence_gate`, AND a per-signal sub-gate). Cost: over-decomposes the gate; the gate's flip-protocol already names both signals concisely; sub-gating each signal introduces dependency-tree depth without benefit. Rejected.
- **Drop the audit-iteration cadence entirely** — close `_v8` with no `_v9` and let any future re-opening of the participant or audience stance trigger a fresh task from scratch. Cost: loses the audit-chain's citable WBS home; contradicts the orchestrator's tech-debt registration policy. The gate addresses the per-pass cost concern that motivated this alternative without losing the chain. Rejected (same reasoning as v1 → v7 Decision §4 alternative C).

Chosen: two named-future-tasks (`_v9` and `_v9_cadence_gate`), named and effort-estimated as above, ready for the closer to register mechanically. The `_v9` task inherits the unchanged lift trigger; its pickup is structurally gated by the `_v9_cadence_gate` sentinel's `complete 100` flip; the gate's flip-protocol is a deliberate human-verified signal check named verbatim in the gate's `note` block. When (and if) the gate is flipped — by signal (i) sweep ceiling firing OR signal (ii) precondition-touching-commit firing — the orchestrator's pick-task pass surfaces `_v9` and the next refinement runs the precondition audit one further iteration on.

## Open questions

(none — all decided)

## Status

**Done** — 2026-05-29.

- Audit-only leaf: both preconditions remain unmet, bit-for-bit identical to `_v7` findings one orchestrator-pick pass earlier — zero matches for pending-axiom-mark identifiers in `apps/participant/src/` and `apps/audience/src/`; zero matching identifiers in `tasks/40-participant-ui.tji` and `tasks/50-audience-and-broadcast.tji`.
- `packages/shell/src/pending-axiom-marks/` does not exist; only the allowed comment reference at `packages/shell/src/facet-pill/participant-color.ts:2` found in shell.
- Moderator symbols confirmed at byte-identical line offsets: `PendingAxiomMark` (line 212), `EMPTY_PENDING_AXIOM_MARKS` (line 225), `projectPendingAxiomMarks` (line 251), `groupPendingAxiomMarksByNode` (line 303) in `apps/moderator/src/graph/selectors.ts`.
- Cadence-rule pickup signals (i) and (ii) both unmet at pick time for the second consecutive iteration; pattern confirmed across the two-iteration evidence window the `_v3` precedent's discipline named; cadence-enforcement-revisit clause inherited from `_v7` Decision §4 fires.
- Escalation chosen: `tj3`-enforceable cadence-gate task per `_v7` Decision §3 path (1) (Decision §3); lift deferred to `shell_package.extract_pending_axiom_mark_projector_v9`, gated by new `shell_package.extract_pending_axiom_mark_projector_v9_cadence_gate` sentinel (Decision §4).
- WBS: `complete 100` added to `shell_package.extract_pending_axiom_mark_projector_v8` in `tasks/27-shell-package.tji`; `_v9` and `_v9_cadence_gate` tasks registered in same file.
