# Lift the annotation entity-detail body into `@a-conversa/shell` — MOVED

> **This refinement has moved.** The canonical copy now lives at
> [`tasks/refinements/post_implementation_audits/extract_annotation_detail_view.md`](../post_implementation_audits/extract_annotation_detail_view.md).
>
> The task moved out of `shell_package` into `post_implementation_audits` on
> 2026-05-30 (parked behind the M8-audits milestone until its second-caller
> trigger gate fires; see ADR 0026 + the canonical refinement's Decision §9 and
> Status block). This file was a stale duplicate left behind by that move; it is
> kept only as a redirect so existing inbound links continue to resolve.
>
> All Decisions, Acceptance criteria, and the live Status / gate-check history
> are maintained in the canonical copy. Do not edit this stub — edit the
> canonical refinement instead.
