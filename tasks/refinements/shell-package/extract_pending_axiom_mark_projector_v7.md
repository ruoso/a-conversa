# Defer the `projectPendingAxiomMarks` lift into `@a-conversa/shell` (v7) — both preconditions remain unmet; cadence-rule pickup signals are unmet too (this pick is a prose-only-cadence violation); close and re-register `_v8` with the same batched cadence

**TaskJuggler entry**: [tasks/27-shell-package.tji](../../27-shell-package.tji) — task `shell_package.extract_pending_axiom_mark_projector_v7` (lines 282-321).
**Effort estimate**: 0.5d (WBS budget — the precondition-audit deliverable plus the cadence-rule-violation observation and the same-cadence re-registration consume ~0.3d together; the lift itself does not land until both UI triggers fire).

## Inherited dependencies

- `shell_package.extract_pending_axiom_mark_projector_v6` (settled 2026-05-29, commit `42ad04b` — [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v6.md`](extract_pending_axiom_mark_projector_v6.md)). **Source of debt.** The strict `!`-edge predecessor and the leaf that pre-registered THIS task verbatim (its Decision §4): same name, same 0.5d effort, same binary lift trigger (*"once BOTH a participant pending-axiom-mark rendering leaf AND an audience pending-axiom-mark rendering leaf are complete 100"*), AND the iteration that closed the v3 cadence-revisit-evidence window by adopting a new **batched-audit pickup cadence** for `_v7` (its Decision §3): pick when (i) 4 or more orchestrator-pick passes have elapsed since `_v6` close, OR (ii) a commit lands touching one of the precondition-bearing files with a matching pending-axiom-mark identifier, whichever comes first. This refinement re-audits the lift trigger one orchestrator-pick pass later, records that the cadence-rule pickup signals were unmet at the time the orchestrator picked this task (signal (i): 1 pass elapsed against a 4-pass ceiling; signal (ii): no precondition-touching commit), and re-registers `_v8` with the unchanged batched cadence — a single prose-only-cadence violation is not yet evidence enough to tighten the policy further.
- `shell_package.extract_pending_axiom_mark_projector_v5` (settled 2026-05-29, commit `b38afc2` — [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v5.md`](extract_pending_axiom_mark_projector_v5.md)). The v5 audit; first iteration of the v3-named cadence-revisit-evidence window. Inherited transitively through `_v6`.
- `shell_package.extract_pending_axiom_mark_projector_v4` (settled 2026-05-29, commit `75c784a` — [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v4.md`](extract_pending_axiom_mark_projector_v4.md)). Inherited transitively through `_v5` → `_v6`.
- `shell_package.extract_pending_axiom_mark_projector_v3` (settled 2026-05-29, commit `e5d9147` — [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v3.md`](extract_pending_axiom_mark_projector_v3.md)). The iteration that established the cadence-revisit precedent (its Decision §4) the `_v6` closure acted on. Inherited transitively through `_v4` → `_v5` → `_v6`; cited here directly because `_v7` is the first iteration governed by the **new** cadence the `_v6` closure scoped on the strength of the v3 precedent.
- `shell_package.extract_pending_axiom_mark_projector_v2` (settled 2026-05-28, commit `9733b28` — [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v2.md`](extract_pending_axiom_mark_projector_v2.md)). The v2 audit. Inherited transitively through `_v3` → `_v4` → `_v5` → `_v6`.
- `shell_package.extract_pending_axiom_mark_projector` (settled 2026-05-28, commit `683143f` — [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector.md`](extract_pending_axiom_mark_projector.md)). The v1 audit — origin of the audit-iteration chain; Decisions §1-§3 framed the audit-and-defer disposition; Decision §4 registered `_v2`. Inherited transitively through `_v2` → `_v3` → `_v4` → `_v5` → `_v6`.
- `shell_package.extract_votes_by_facet_projector_v2` (settled 2026-05-28, commit `8c7087b` — [`tasks/refinements/shell-package/extract_votes_by_facet_projector_v2.md`](extract_votes_by_facet_projector_v2.md)). The sibling `_v2` lift that DID fire — **reference precedent for what a `_vN` lift looks like when its preconditions DO get met.** The asymmetry with this chain is unchanged at `_v7` audit time and continues to be the durable reason this chain's `_vN` continues to defer while the votes-by-facet `_v2` terminated by lifting. v2 + v3 + v4 + v5 + v6 Decisions §1 already recorded this asymmetry in full; this `_v7` inherits the rejection verbatim. Not a `.tji` `depends` edge — prose-only authority.
- Prose-only context (NOT a `.tji` edge): `shell_package.extract_facet_pill` (settled — [`tasks/refinements/shell-package/extract_facet_pill.md`](extract_facet_pill.md)) — source of the three-caller policy. One- and two-caller helpers stay per-workspace; lift fires when the third caller materializes with a convergent shape. `projectPendingAxiomMarks` has one caller (moderator-only) at `_v7` commit time; the gap to the three-caller floor is unchanged from `_v6` audit time.
- Prose-only context (NOT a `.tji` edge): `shell_package.extract_cytoscape_projectors` (settled — [`tasks/refinements/shell-package/extract_cytoscape_projectors.md`](extract_cytoscape_projectors.md)) — pins the lift recipe (`packages/shell/src/<vocabulary>/` sibling directory, consolidated Vitest suite, root re-export block, atomic-move-no-shim per-workspace import rewire) that the future `_v7+N` (or terminating) lift will follow verbatim when the trigger fires.
- Prose-only context (NOT a `.tji` edge): `shell_package.shell_axiom_marks_extraction` (settled — [`tasks/refinements/shell-package/shell_axiom_marks_extraction.md`](shell_axiom_marks_extraction.md)) — establishes `packages/shell/src/axiom-marks/` as the canonical home for the committed projector trio, AND the sibling-directory convention this audit chain's future lift would inherit (`packages/shell/src/pending-axiom-marks/`). Pending and committed variants live as separate sibling directories, not as a merged namespace (v1 Decision §1 alternative C rejected — inherited through `_v2` → `_v3` → `_v4` → `_v5` → `_v6` → here unchanged).
- Prose-only context (NOT a `.tji` edge): `moderator_ui.mod_axiom_mark_pending_render` (settled — [`tasks/refinements/moderator-ui/mod_axiom_mark_pending_render.md`](../moderator-ui/mod_axiom_mark_pending_render.md)) — the moderator leaf that landed the four pending-axiom-mark symbols. Decision §1 documents the two-terminator handling (`commit` + `meta-disagreement-marked`, both with `payload.target === 'proposal'`); Decision §2 the no-per-participant-uniqueness rule; Decision §4 scoped the trio as moderator-internal at authoring time, coupled to the moderator's proposal-author affordance.
- Prose-only context (NOT a `.tji` edge): `participant_ui.part_axiom_mark_decoration` (settled — [`tasks/refinements/participant-ui/part_axiom_mark_decoration.md`](../participant-ui/part_axiom_mark_decoration.md)) — **explicitly excludes pending-axiom-mark rendering** ([`:27`](../participant-ui/part_axiom_mark_decoration.md#L27)); at-a-glance signal is "ratified bedrock"; pending is deferred to a hypothetical `part_pending_proposals.*` group. The `part_pending_proposals.*` group exists in the WBS ([`tasks/40-participant-ui.tji:210`](../../40-participant-ui.tji#L210)) but its shipped leaves landed a list-view pane, NOT a graph-canvas pending-axiom-mark overlay — the principled-absence is unrevised at `_v7` audit time. The sibling subtree `part_axiom_mark_from_tablet` (lines 208-245) is `complete 100` for **committed**-axiom-mark workflow only; its deferred-∞ sub-tasks (`part_mark_axiom_action`, `part_axiom_mark_proposal`) name pending-mark **authoring** as future work but not pending-mark **rendering**.
- Prose-only context (NOT a `.tji` edge): `audience.aud_axiom_mark_decoration` (settled — [`tasks/refinements/audience/aud_axiom_mark_decoration.md`](../audience/aud_axiom_mark_decoration.md)) — **explicitly excludes pending-axiom-mark rendering** ([`:38`](../audience/aud_axiom_mark_decoration.md#L38)); a future polish leaf (`aud_axiom_mark_pending_render`, ~1d) is named in prose but explicitly **NOT pre-registered** ("registered only when a concrete trigger materializes, not on a calendar date"). The principled-absence is unrevised at `_v7` audit time. The sibling subtree `audience.aud_graph_rendering` is 30/52 `complete 100`; the remaining leaves include `aud_pending_proposals_pane` and `aud_pending_proposals_pending_votes` (deferred), but no leaf scopes pending-axiom-mark **graph-canvas** rendering.
- Prose-only context (NOT a `.tji` edge): ADR 0030 §9 (per-facet vote keying) — the structural sub-kinds of `target='proposal'` vs `target='facet'` that the projector's two terminator branches resolve through. The proposal-keyed terminator arm's authority is unchanged by this audit and would be honored verbatim by the future lift.

## What this task is

The half-day precondition-audit AND cadence-rule-observation deliverable for the **still-deferred** `projectPendingAxiomMarks` shell-extraction, at audit-iteration 7. The predecessor `_v6` audit ([`extract_pending_axiom_mark_projector_v6.md`](extract_pending_axiom_mark_projector_v6.md), settled 2026-05-29 at commit `42ad04b`) registered THIS `_v7` task with the unchanged binary lift trigger ("both UI leaves `complete 100`") AND a **new batched-audit pickup cadence** (v6 Decision §3 + §4): pick when (i) 4 or more orchestrator-pick passes have elapsed since `_v6` close, OR (ii) a commit lands touching one of the precondition-bearing files with a matching pending-axiom-mark identifier, whichever comes first. The orchestrator's pick-task pass surfaced this `_v7` task one pick after `_v6` closed — a state in which neither cadence signal has fired (Decision §3 records this in full; the short answer is that the batched-audit cadence is documented in prose-and-`note` form, not in `tj3`-enforceable WBS structure, so the orchestrator's mechanical scheduler treats `_v7` as pickable as soon as its `!`-edge predecessor closed). The pass cannot verify either (i) whether the named lift-preconditions have flipped, or (ii) whether the cadence-rule pickup signals were honored. Both verifications are this refinement's activity.

This refinement audits the lift trigger at refinement time and confirms **both preconditions remain unmet AND remain principledly absent**, identically to the `_v6` finding one orchestrator-pick pass earlier. The findings are bit-for-bit identical to `_v6`'s, because the only commit landed between `_v6`'s commit (`42ad04b`) and this `_v7` audit pass is `_v6`'s own close commit (which touches [`tasks/27-shell-package.tji`](../../27-shell-package.tji) and [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v6.md`](extract_pending_axiom_mark_projector_v6.md) — neither in the cadence-(ii) signal's precondition-bearing file list, by design):

- **Precondition (a) — participant pending-axiom-mark rendering leaf — unmet, unchanged.** A grep of [`apps/participant/src/`](../../../apps/participant/src/) for `pendingAxiomMark` / `PendingAxiomMark` / `pending-axiom-mark` / `projectPendingAxiomMarks` / `EMPTY_PENDING_AXIOM_MARKS` at `_v7` refinement time returns **zero matches** — no participant code imports, consumes, or re-implements the projector. A grep of [`tasks/40-participant-ui.tji`](../../40-participant-ui.tji) for `pending_axiom` / `axiom_mark_pending` / `axiom_pending` returns **zero matches**: no task identifier names a participant pending-axiom-mark rendering leaf, complete or otherwise. The lone `part_pending_proposals` task ([`:210`](../../40-participant-ui.tji#L210)) is a list-view pane, not a graph-canvas pending-axiom-mark overlay. [`part_axiom_mark_decoration.md:27`](../participant-ui/part_axiom_mark_decoration.md#L27) reads the same line at the same offset as it did at `_v6` audit time; the refinement's Status line ("Done — 2026-05-17") still has no amendment. The participant's principled-absence decision (committed-only "ratified bedrock") has not been revised. The trigger cannot fire until a new participant leaf scopes graph-canvas pending-axiom-mark rendering; no such leaf exists.
- **Precondition (b) — audience pending-axiom-mark rendering leaf — unmet, unchanged.** A grep of [`apps/audience/src/`](../../../apps/audience/src/) for `pendingAxiomMark` / `PendingAxiomMark` / `pending-axiom-mark` returns **zero matches** — no audience code imports, consumes, or re-implements the projector. A grep of [`tasks/50-audience-and-broadcast.tji`](../../50-audience-and-broadcast.tji) for `aud_axiom_mark_pending` / `aud_pending_axiom_mark` / `pending_axiom_mark` returns **zero matches**. The deferred leaves under `audience.aud_graph_rendering` (`aud_pending_proposals_pane`, `aud_pending_proposals_pending_votes`) scope a future pending-proposals pane but not pending-axiom-mark **graph-canvas** rendering. [`aud_axiom_mark_decoration.md:38`](../audience/aud_axiom_mark_decoration.md#L38) is byte-identical to its `_v6` audit-time content; the prose-named future polish leaf (`aud_axiom_mark_pending_render`, ~1d) remains explicitly NOT pre-registered. The refinement's Status line ("Done — 2026-05-28") still has no amendment. The audience's principled-absence decision is unrevised. The trigger cannot fire until a new audience leaf scopes pending-axiom-mark rendering; no such leaf exists.

Both surfaces have **continuously, identically, and independently maintained** their committed-only "ratified bedrock" framing across the v1 → v2 → v3 → v4 → v5 → v6 → v7 audit interval. The only commit landed between `_v6` close (`42ad04b`) and this `_v7` audit is `_v6`'s own close commit; the moderator's four pending-axiom-mark symbols remain at byte-identical line offsets in [`apps/moderator/src/graph/selectors.ts`](../../../apps/moderator/src/graph/selectors.ts) (line 212 `PendingAxiomMark`, line 225 `EMPTY_PENDING_AXIOM_MARKS`, line 251 `projectPendingAxiomMarks`, line 303 `groupPendingAxiomMarksByNode`). The principled-absence the v1 → v6 audits all recorded as the reason for deferral is structurally unchanged.

Three deliverables sit inside this leaf:

1. The audit record itself: a confirmation that the deferral remains correct one further audit-iteration on, that the trigger semantics are unchanged, and that the moderator's four-symbol module-internal state is preserved bit-for-bit since `_v6`.
2. **The cadence-rule-violation observation.** The new batched-audit cadence the `_v6` closer scoped (every 4 pick-passes OR a precondition-touching commit) was not honored by the orchestrator's pick-task pass — `_v7` was picked at pick-pass 1, with signal (ii) also unfired. The refinement records this observation in Decision §3, identifies it as a known limitation of the cadence-rate mechanism (prose-only-cadence in a `.tji` `note` block has no `tj3`-enforceable scheduling effect — the orchestrator's pick-task pass walks `complete 100` + `depends` edges only), and decides NOT to tighten the cadence rule further on the strength of a single observation (one violation may be a one-off; tightening on it risks over-correction).
3. **The same-cadence `_v8` re-registration.** Decision §4 registers `_v8` with the same lift trigger AND the same batched-audit cadence as `_v7`, plus an explicit "third identical-findings iteration" clause: if `_v8`'s findings are again bit-for-bit identical, that iteration's refinement re-evaluates whether to escalate the cadence-enforcement mechanism (e.g., add a `tj3`-enforceable structural delay, or fold the chain into a non-leaf "watcher" pattern), but not before.

After this leaf:

- [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v7.md`](extract_pending_axiom_mark_projector_v7.md) (this file) exists as the audit record AND the cadence-rule-violation observation record AND the `_v8` registration record.
- The four moderator-local symbols stay where they are: [`apps/moderator/src/graph/selectors.ts`](../../../apps/moderator/src/graph/selectors.ts) — `PendingAxiomMark` interface (line 212), `EMPTY_PENDING_AXIOM_MARKS` (line 225), [`projectPendingAxiomMarks` at line 251](../../../apps/moderator/src/graph/selectors.ts#L251), [`groupPendingAxiomMarksByNode` at line 303](../../../apps/moderator/src/graph/selectors.ts#L303). Byte-identical to the `_v6` audit-time snapshot.
- The WBS task `shell_package.extract_pending_axiom_mark_projector_v7` is marked `complete 100` by the closer (the audit + the cadence-observation + the re-registration are the deliverable for this leaf's budget). The closer registers ONE follow-up named-future-task in [`tasks/27-shell-package.tji`](../../27-shell-package.tji) — `shell_package.extract_pending_axiom_mark_projector_v8` — with the **same batched-audit cadence semantics** baked into its trigger (Decision §4 names it crisply: same lift trigger, same pickup cadence as `_v7`, with the cadence-enforcement-revisit clause).
- The next time the precondition trigger appears to fire (a participant or audience pending-axiom-mark rendering refinement reaches its source-of-debt stage), OR the next time the batched-audit cadence elapses (whichever comes first), the implementer reads this audit first and acts on the named-future-task `_v8` — not on this `_v7` leaf, which is closed.

Out of scope (explicitly NOT done here):

- **The lift itself.** No `packages/shell/src/pending-axiom-marks/` directory is created. No symbol is moved. No callers rewire imports. The shell substrate gains no new entry. The shell's existing `packages/shell/src/axiom-marks/` directory (the committed-mark sibling) is untouched; in particular, the pending and committed variants are NOT merged into a single namespace (v1 Decision §1 alternative C rejected — inherited through `_v2` → `_v3` → `_v4` → `_v5` → `_v6` → here unchanged).
- **Relaxing the three-caller policy on the strength of "the methodology question is settled in the moderator-only direction."** Already rejected by `_v2` + `_v3` + `_v4` + `_v5` + `_v6` Decision §1 (alternative B) under the votes-by-facet-asymmetry rationale (votes-by-facet relaxed because an in-flight methodology task produced two byte-identical projector bodies across two existing callers; pending-axiom-marks has one body by methodology design — the inverse situation). The asymmetry is unchanged at `_v7` audit time; the rejection is inherited verbatim (Decision §1 of this refinement records the inheritance).
- **Pre-registering the participant `part_pending_proposals.*` graph-canvas overlay leaf or the audience `aud_axiom_mark_pending_render` leaf.** Both surfaces' refinements explicitly declined to register those leaves (v1 + v2 + v3 + v4 + v5 + v6 Decision §2, inherited verbatim). Pre-registering them from a shell-package `_v7` audit would invert the WBS's dependency direction. Rejected as alternative B in Decision §2.
- **A parametric / merged-axiom-mark projector** (`projectAxiomMarks` extended to optionally produce pending entries via an options bag). Already rejected by v1 Decision §1 alternative C; inherited through `_v2` → `_v3` → `_v4` → `_v5` → `_v6` to here.
- **`<PendingAxiomMarkBadge>` component itself.** Out of this leaf's name list (the WBS scope is the projector trio + the type + the empty-array constant — not the badge component). Even when the lift fires, the badge's home is decided by the second and third callers' actual consumption shape (a separate question for the future lift's refinement, whenever that lands). Out of scope here; not registered as a named-future-task today.
- **Changes to the moderator's projector behavior or its `<PendingAxiomMarkBadge>` consumer.** The four moderator-local symbols and their three consumers ([`GraphCanvasPane.tsx`](../../../apps/moderator/src/graph/GraphCanvasPane.tsx), [`StatementNode.tsx`](../../../apps/moderator/src/graph/StatementNode.tsx), [`PendingAxiomMarkBadge.tsx`](../../../apps/moderator/src/graph/PendingAxiomMarkBadge.tsx)) stay byte-identical at this commit. No re-comment, no signature change, no `Readonly` widen, no extraction of an `EventLog`-style adapter — same constraint as v1 + v2 + v3 + v4 + v5 + v6.
- **A unification of the committed and pending Cucumber surfaces or any new BDD pin.** No Cucumber scenario lands. No protocol seam is crossed. The audit is documentation-only.
- **`projectVotesByProposal`** ([`selectors.ts`](../../../apps/moderator/src/graph/selectors.ts)) and other moderator-only single-caller helpers. Already explicitly out of scope per the v1 + v2 + v3 + v4 + v5 + v6 audits. Not re-litigated here.
- **Tightening the cadence rule on the strength of one cadence-rule-violation observation.** Even though the orchestrator's pick-task pass picked `_v7` at pick-pass 1 (in violation of the prose-only cadence the `_v6` closer scoped), a single observation is insufficient to escalate the enforcement mechanism. Decision §3 records the violation and decides NOT to tighten on this iteration's evidence; Decision §4 carries the same cadence to `_v8` and reserves the escalation question for `_v8`'s refinement if the violation pattern repeats. Rejected as Decision §3 alternative B.
- **Dropping the audit-iteration chain entirely** (closing `_v7` with no successor). Even though the cadence is being violated, abandoning the chain loses the citable home for the deferral. Rejected as Decision §4 alternative C.
- **Binding the cadence to a wall-clock calendar date** (e.g., "next audit after 2026-08-01"). Already rejected by `_v5` Decision §3 alternative D and `_v6` Decision §3 alternative D; inherited verbatim. The cadence remains iteration-count-based (with the change-signal pickup).
- **Restructuring the WBS so the cadence is `tj3`-enforceable** — e.g., introducing a `purge` or `gate` task that holds `_v8`'s `depends` edge open until N pick-passes have elapsed. The orchestrator / closer own WBS shape (per the refinement-writer brief); the refinement does not edit `.tji` files. The Decision §3 observation is recorded so a future closer / orchestrator pass can decide whether to escalate enforcement; the refinement does not implement the escalation itself.

## Why it needs to be done

The `_v6` audit closed with the explicit pre-registration of THIS `_v7` task at unchanged lift-trigger semantics (both UI leaves `complete 100`), AND with the explicit adoption of a batched-audit pickup cadence (every 4 pick-passes OR a precondition-touching commit, whichever comes first; see `_v6` Decision §3). The orchestrator's pick-task pass surfaced this task one pick after `_v6` closed — a state in which neither cadence signal had fired. The pass cannot verify either (i) whether the participant + audience leaves have materialized between `_v6`'s commit (`42ad04b`, 2026-05-29) and this `_v7`'s commit, or (ii) whether the cadence rule has been honored by the pick. Both verifications are the refinement-stage activity this leaf delivers, alongside the same-cadence `_v8` re-registration.

Five observations make the audit valuable (rather than a no-op formality at one further iteration's distance from the originating v1):

1. **The `_v6` audit identified the same revision path the v1 → v5 audits named (the participant and audience refinements would have to explicitly revise their principled-absence decisions), and one further audit-iteration is the right place to re-check whether that path has been walked.** A search of the two work-streams' refinements + their `.tji` task blocks at `_v7` commit time shows zero revisions: [`part_axiom_mark_decoration.md`](../participant-ui/part_axiom_mark_decoration.md) Status line says "Done — 2026-05-17" with no amendments; [`aud_axiom_mark_decoration.md`](../audience/aud_axiom_mark_decoration.md) Status line says "Done — 2026-05-28" with no amendments. No new participant leaf scoping graph-canvas pending-axiom-mark rendering; no new audience leaf scoping `aud_axiom_mark_pending_render` or any analog; no commits touching either surface's stance on pending-axiom-marks (the only commit between `42ad04b` and this `_v7` audit is `42ad04b` itself, the `_v6` close). The principled-absence is durable across seven audit iterations, not transient.
2. **The sibling `extract_votes_by_facet_projector_v2` lift remains the load-bearing asymmetry citation.** The v2 + v3 + v4 + v5 + v6 audits' Decision §1 each recorded in full why that `_v2` lifted (in-flight methodology task closed the divergence half; two byte-identical projector bodies across two existing callers gave evidence weight to relaxing the third-workspace half) and why this audit chain's pending-axiom-mark `_v2` → `_v6` did NOT (the methodology is settled in the principled-absence direction; the trigger is genuinely binary on UI work that is not scheduled). The asymmetry is unchanged at `_v7` audit time and continues to be the durable reason this chain's `_vN` continues to defer while the votes-by-facet `_v2` terminated by lifting. The audit records this so a future reader sees at one glance why the two chains diverged at this disposition step without having to re-derive the methodology context from the source refinements.
3. **Closing this `_v7` leaf cleanly with the same batched cadence prevents pick-task queue churn while preserving the cadence-rule-violation observation.** Leaving the leaf open means every pick-task pass either re-refines it or skips it ambiguously. The closer marks the leaf `complete 100`; the future-trigger work is held by a freshly-named `_v8` follow-up (Decision §4). The pick-task queue retains a citable home for the deferral.
4. **The cadence-rule-violation observation is exactly the kind of evidence the cadence-revisit precedent was set to gather — but the precedent's two-iteration window discipline applies here too.** The v3 precedent was scoped as "across a two-iteration evidence window, NOT at any single iteration"; that discipline carries forward to **how** the new cadence's enforcement is evaluated. One iteration of pick-pass-1 pickup is observation, not enough evidence to escalate. Decision §3 records the observation, names a structural reason (prose-only cadence in `.tji` `note` blocks has no `tj3`-enforceable scheduling effect — orchestrator pick-pass walks `complete 100` + `depends` edges only), and reserves the escalation question for `_v8`'s refinement if the pattern repeats. Acting on a single observation would over-correct the precedent's own evidence-gathering discipline.
5. **Recording the new batched cadence's actual enforcement behavior produces durable documentation about the cadence policy's mechanical limits.** Future maintainers reading this chain will see that prose-only cadence rules in `.tji` `note` blocks document intent but do not slow the orchestrator's pick-task pass mechanically; if a future iteration of this chain (or any other chain adopting a similar policy) needs cadence-enforcement, the refinement here names two paths (the `tj3`-enforceable gate task; the "watcher" pattern), neither of which is implemented today but both of which are explicit options. The observation is more useful as durable documentation than as immediate action.

The follow-on benefits of the audit:

- **Prevents premature ossification.** The three-caller policy's whole point is to wait for empirical convergence rather than to design-shape-and-hope. Lifting `projectPendingAxiomMarks` into the shell today as a single-caller move "in case" the second and third callers eventually materialize would create a shell-side projector whose only consumer is the moderator's `GraphCanvasPane` — exactly the anti-pattern `extract_cytoscape_projectors` Decision §1 rejected for the participant boolean+count helpers and that `extract_facet_pill` Decision §2 set the three-caller policy to prevent. The v1 → v6 audits rejected this for the same reasons; the `_v7` re-audit confirms the reasoning still holds.
- **Records the named-future-task path crisply at the new cadence going forward.** The orchestrator brief's tech-debt registration policy asks every deferred task to be named with effort + description; Decision §4 supplies both for the `_v8` follow-up, freshly stating the trigger semantics AND the same batched-audit pickup cadence (with the cadence-enforcement-revisit clause). The deferral remains visible on the WBS.
- **Pins the recipe in advance.** When the trigger does fire (likely 2-4 quarters out, after the participant's pending-proposals pane expands or the audience's broadcast-feedback iteration surfaces a need), the implementer of the terminating-lift refinement reads the audit chain and follows the `shell_axiom_marks_extraction` + `extract_cytoscape_projectors` + `extract_votes_by_facet_projector_v2` recipe verbatim — no design re-derivation needed across the chain.
- **Names the cadence-enforcement escalation path without implementing it.** Decision §3 names two enforceable-cadence mechanisms (a `tj3`-enforceable gate task; a non-leaf "watcher" pattern) and explicitly leaves implementation for `_v8` or later, conditioned on a repeated violation pattern. The refinement is the durable-documentation layer; the escalation is held until evidence justifies it.

## Inputs / context

### ADRs

- [ADR 0022 — No throwaway verifications](../../../docs/adr/0022-no-throwaway-verifications.md). The Vitest layer for the pending-axiom-mark projector stays where it is — the moderator's [`apps/moderator/src/graph/selectors.test.ts`](../../../apps/moderator/src/graph/selectors.test.ts) cases at the `describe('projectPendingAxiomMarks')` and `describe('groupPendingAxiomMarksByNode')` blocks are the regression pin for the moderator surface's local behavior. No throwaway smoke script is introduced here; this audit produces no executable artifact.
- [ADR 0026 — Micro-frontend root app](../../../docs/adr/0026-micro-frontend-root-app.md). When the lift does fire (`_v8` or beyond), the shell's `packages/shell/src/pending-axiom-marks/` directory is the architecturally-correct home per the ADR's substrate posture (every UI surface already depends on `@a-conversa/shell` at runtime). Today the audit defers that move; the substrate-posture rationale stays intact for the future lift.
- [ADR 0030 — Per-facet vote keying](../../../docs/adr/0030-per-facet-vote-keying.md). §9 (the structural sub-kinds of `target='proposal'` vs `target='facet'`) is what the projector's two terminator branches resolve through ([`apps/moderator/src/graph/selectors.ts:278,290`](../../../apps/moderator/src/graph/selectors.ts#L278) — commit + meta-disagreement-marked `target='proposal'` events remove pending entries); the proposal-keyed terminator arm is bound by this ADR. Unchanged by this audit; bound for the future lift to honor verbatim.

### Prior shell-package decisions this audit is bound by

- [`extract_pending_axiom_mark_projector_v6.md`](extract_pending_axiom_mark_projector_v6.md) — the immediately-preceding audit. Decisions §1-§4 frame the audit-and-defer disposition, the no-pre-registration policy, the cadence-revisit-evidence-window-closure-and-act disposition, and the `_v7` registration shape (with the new batched-audit cadence). This `_v7` audit honors §1, §2, and §4 verbatim — same audit shape, same close-and-defer disposition for the lift trigger, same crisp follow-up registration shape (now for `_v8`). §3 (the new batched cadence) is the rule this `_v7`'s pickup was supposed to honor; the cadence-rule-violation observation (Decision §3 of this refinement) records that the orchestrator's pick-task pass did not honor it and explains why.
- [`extract_pending_axiom_mark_projector_v5.md`](extract_pending_axiom_mark_projector_v5.md) — the `_v5` audit; first iteration of the v3-named cadence-revisit-evidence window. Inherited transitively through `_v6`.
- [`extract_pending_axiom_mark_projector_v4.md`](extract_pending_axiom_mark_projector_v4.md) — the v4 audit. Inherited transitively through `_v5` → `_v6`.
- [`extract_pending_axiom_mark_projector_v3.md`](extract_pending_axiom_mark_projector_v3.md) — the v3 audit; the iteration that established the cadence-revisit precedent (its Decision §4) the `_v6` closure acted on. Inherited transitively through `_v4` → `_v5` → `_v6`; cited directly here because `_v7` is the first iteration governed by the new cadence the v3 precedent's closure scoped.
- [`extract_pending_axiom_mark_projector_v2.md`](extract_pending_axiom_mark_projector_v2.md) — the v2 audit. Inherited transitively through `_v3` → `_v4` → `_v5` → `_v6`.
- [`extract_pending_axiom_mark_projector.md`](extract_pending_axiom_mark_projector.md) — the v1 audit. Decisions §1-§3 framed the audit-and-defer disposition; Decision §4 pre-registered `_v2`. Inherited transitively through `_v2` → `_v3` → `_v4` → `_v5` → `_v6`.
- [`extract_votes_by_facet_projector_v2.md`](extract_votes_by_facet_projector_v2.md) Decision §1 — the lift-relaxation precedent. Pending-axiom-marks structurally do not match (recorded in `_v2` + `_v3` + `_v4` + `_v5` + `_v6` Decision §1; inherited here verbatim).
- [`extract_cytoscape_projectors.md`](extract_cytoscape_projectors.md) Decision §3 — the original source-of-debt for the v1 audit, inherited transitively.
- [`extract_facet_pill.md`](extract_facet_pill.md) Decision §2 — the three-caller policy.
- [`shell_axiom_marks_extraction.md`](shell_axiom_marks_extraction.md) — the directory + barrel + root-export shape the future lift will inherit.

### Prior moderator + participant + audience decisions on pending-axiom-marks

- [`mod_axiom_mark_pending_render.md`](../moderator-ui/mod_axiom_mark_pending_render.md) Decisions §1 (two-terminator handling), §2 (no per-participant uniqueness), §4 (pending-projector trio scoped as moderator-internal — coupled to the moderator's proposal-author affordance).
- [`part_axiom_mark_decoration.md`](../participant-ui/part_axiom_mark_decoration.md) [`:27`](../participant-ui/part_axiom_mark_decoration.md#L27) — explicit exclusion of pending-axiom-mark rendering from the participant surface; at-a-glance signal is "ratified bedrock" only. Stance unchanged at `_v7` commit time (Status line: Done — 2026-05-17; no amendments).
- [`aud_axiom_mark_decoration.md`](../audience/aud_axiom_mark_decoration.md) [`:38`](../audience/aud_axiom_mark_decoration.md#L38) — explicit exclusion of pending-axiom-mark rendering from the audience surface; future polish leaf `aud_axiom_mark_pending_render` (~1d) named in prose but explicitly NOT pre-registered. Stance unchanged at `_v7` commit time (Status line: Done — 2026-05-28; no amendments).

### Live code at refinement time

#### Moderator surface — canonical (and only) pending-axiom-mark projector; unchanged since `_v6`

[`apps/moderator/src/graph/selectors.ts`](../../../apps/moderator/src/graph/selectors.ts) defines the same four contiguous symbols the `_v6` audit recorded, at the same line offsets:

- `PendingAxiomMark` interface at line 212 (the readonly `{ proposalEventId, nodeId, participantId, proposedAt }` shape).
- `EMPTY_PENDING_AXIOM_MARKS: readonly PendingAxiomMark[]` at line 225 (the frozen-empty reference for per-node fallbacks).
- [`projectPendingAxiomMarks(events) → PendingAxiomMark[]` at line 251](../../../apps/moderator/src/graph/selectors.ts#L251) — walks events once with a `Map<proposalEventId, PendingAxiomMark>`; records on `proposal` events whose inner kind is `axiom-mark`; removes on `commit` and `meta-disagreement-marked` events whose `payload.target === 'proposal'` (per ADR 0030 §9); returns surviving entries in proposal-arrival order.
- [`groupPendingAxiomMarksByNode(marks) → Map<string, PendingAxiomMark[]>` at line 303](../../../apps/moderator/src/graph/selectors.ts#L303) — buckets pending marks by their target node id for O(1) per-node enrichment lookups.

Line offsets are byte-identical to the `_v6` audit's snapshot — the only commit between `42ad04b` (`_v6` close) and this `_v7` audit is `42ad04b` itself, which touched only `tasks/27-shell-package.tji` and `tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v6.md`. Bodies are byte-identical. The header comment block immediately above the projector continues to document both the per-arrival ordering rule and the no-per-participant-uniqueness rule.

Callers (sole surface — moderator, unchanged since `_v6`):

- [`apps/moderator/src/graph/GraphCanvasPane.tsx`](../../../apps/moderator/src/graph/GraphCanvasPane.tsx) — imports `EMPTY_PENDING_AXIOM_MARKS`, `groupPendingAxiomMarksByNode`, and `projectPendingAxiomMarks`; runs the projector to produce the per-node fallback the moderator's graph canvas reads.
- [`apps/moderator/src/graph/StatementNode.tsx`](../../../apps/moderator/src/graph/StatementNode.tsx) — imports the `<PendingAxiomMarkBadge>` component and a type-only `PendingAxiomMark` import; consumes on `pendingAxiomMarks` prop downstream.
- [`apps/moderator/src/graph/PendingAxiomMarkBadge.tsx`](../../../apps/moderator/src/graph/PendingAxiomMarkBadge.tsx) — type-only `PendingAxiomMark` import — the dashed-faded badge component that paints each pending mark.

Test coverage: [`apps/moderator/src/graph/selectors.test.ts`](../../../apps/moderator/src/graph/selectors.test.ts) `describe('projectPendingAxiomMarks')` + `describe('groupPendingAxiomMarksByNode')` blocks (coverage unchanged from v2/v3/v4/v5/v6 audit-time snapshots).

#### Participant surface — no pending-axiom-mark code at this commit; no scheduled work

[`apps/participant/src/`](../../../apps/participant/src/) at `_v7` refinement time: a recursive grep for `pendingAxiomMark` / `PendingAxiomMark` / `pending-axiom-mark` / `projectPendingAxiomMarks` / `EMPTY_PENDING_AXIOM_MARKS` returns **zero matches** — bit-for-bit identical to the `_v6` finding. No file in `apps/participant/src/graph/` or `apps/participant/src/proposals/` consumes the projector; no `<PendingAxiomMarkOverlay>` analog exists; no `PendingAxiomMarkBadge`-equivalent is rendered. The `part_axiom_mark_from_tablet` subtree at [`tasks/40-participant-ui.tji:208-245`](../../40-participant-ui.tji#L208) is `complete 100` for committed-axiom-mark workflow only; its deferred-∞ sub-tasks (`part_mark_axiom_action`, `part_axiom_mark_proposal`) name pending-mark authoring as future work but not pending-mark rendering. The `part_pending_proposals` task at [`tasks/40-participant-ui.tji:210`](../../40-participant-ui.tji#L210) shipped a list-view pane — not a graph-canvas pending-axiom-mark overlay or any consumer of `projectPendingAxiomMarks`. No follow-up task identifier in `tasks/40-participant-ui.tji` matches `part_axiom_mark_pending` / `part_pending_axiom_mark` / `pending_axiom_mark`. Precondition (a): unmet, unchanged from `_v6`.

#### Audience surface — no pending-axiom-mark code; no scheduled work

[`apps/audience/src/`](../../../apps/audience/src/) at `_v7` refinement time: a recursive grep for `pendingAxiomMark` / `PendingAxiomMark` / `pending-axiom-mark` returns **zero matches** — bit-for-bit identical to the `_v6` finding. No file in `apps/audience/src/graph/` consumes the projector; no `<AudiencePendingAxiomMarkOverlay>` analog exists. A grep of [`tasks/50-audience-and-broadcast.tji`](../../50-audience-and-broadcast.tji) for `aud_axiom_mark_pending` / `aud_pending_axiom_mark` / `pending_axiom_mark` returns **zero matches**. The deferred leaves under `audience.aud_graph_rendering` (`aud_pending_proposals_pane`, `aud_pending_proposals_pending_votes`, `aud_annotation_rendering`, `aud_annotation_rendering_edges`) scope a pending-proposals pane and annotation rendering but NOT pending-axiom-mark graph-canvas rendering. The future polish leaf named in prose ([`aud_axiom_mark_decoration.md:38`](../audience/aud_axiom_mark_decoration.md#L38) — `aud_axiom_mark_pending_render`, ~1d) remains explicitly NOT registered today. Precondition (b): unmet, unchanged from `_v6`.

#### Existing shell substrate — `axiom-marks/` sibling directory exists; `pending-axiom-marks/` does not

[`packages/shell/src/`](../../../packages/shell/src/) at `_v7` refinement time hosts the existing substrate directories — `annotations/`, `auth/`, `axiom-marks/` (committed; lifted by `shell_axiom_marks_extraction`), `error-mapper/`, `facet-pill/`, `facet-status/`, `i18n/`, `login-logout/`, `mount-contract/`, `screen-name/`, `slots/`, `votes-by-facet/`, `ws/`, etc. **No `pending-axiom-marks/` directory exists.** A grep of [`packages/shell/src/`](../../../packages/shell/src/) for `PendingAxiomMark` returns only an incidental comment reference at [`facet-pill/participant-color.ts:2`](../../../packages/shell/src/facet-pill/participant-color.ts#L2) (a doc-line naming `<PendingAxiomMarkBadge>` as a downstream consumer of the deterministic `axiomMarkColorFor` palette — not an active import; same allowed comment-only reference as `_v2` → `_v6`). The hosting infrastructure (sibling-directory layout + root re-export pattern + ready re-export block slot in `packages/shell/src/index.ts`) is ready for the future lift.

#### Cadence-rule pickup-signal state at refinement time

The `_v6` Decision §3 + §4 named two pickup signals for `_v7`; both are observable at this refinement time:

- **Signal (i) — 4 or more orchestrator-pick passes elapsed since `_v6` close.** `_v6` closed at commit `42ad04b` on 2026-05-29. The orchestrator's pick-task pass surfaced `_v7` on the **first** pick after `_v6` closed. 1 pass < 4 passes; signal (i) has not fired.
- **Signal (ii) — a commit landed touching `apps/{participant,audience}/src/`, `tasks/{40-participant-ui,50-audience-and-broadcast}.tji`, or `tasks/refinements/{participant-ui/part_axiom_mark_decoration,audience/aud_axiom_mark_decoration}.md`, AND adding/removing/modifying a line matching the pending-axiom-mark identifier set (case-insensitive).** The only commit landed since `_v6` close (`42ad04b`) is `42ad04b` itself. `42ad04b` touched [`tasks/27-shell-package.tji`](../../27-shell-package.tji) and [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v6.md`](extract_pending_axiom_mark_projector_v6.md) — **neither in the precondition-bearing file list** (that list intentionally excluded shell-package's own files to prevent the chain's own close-commits from looping back as pickup signals). Signal (ii) has not fired.

Both signals unmet; the orchestrator's pick-task pass picked `_v7` anyway. The structural reason is that the cadence is documented in prose-and-`note` form on the `_v7` task block ([`tasks/27-shell-package.tji:301-312`](../../27-shell-package.tji#L301)) — it is human-readable narrative, not a `tj3`-enforceable structural constraint. The orchestrator's pick-task pass walks `complete 100` markers and `depends` edges; with `_v6` `complete 100` and `_v7`'s only `depends` edge being `!extract_pending_axiom_mark_projector_v6`, `_v7` is mechanically pickable as soon as `_v6` closes. The cadence-policy text in the `note` block does not slow the scheduler; it documents intent for the refinement-writer reading the task. Decision §3 records this in full.

## Constraints / requirements

- **Do not lift anything.** Both preconditions (a) and (b) named by the v1 audit's Decision §4 and re-confirmed by `_v2` → `_v6` Decisions §1 must be met before any shell directory is created or any caller is rewired. Both are unmet at this leaf's commit time, in bit-for-bit identical state to the `_v6` audit one orchestrator-pick pass earlier. The audit is part of the deliverable; the cadence-rule-violation observation (Decision §3) and the same-cadence `_v8` re-registration (Decision §4) are the rest. (Decision §1.)
- **Do not relax the lift trigger.** The asymmetry with the sibling `extract_votes_by_facet_projector_v2`'s relaxable trigger is unchanged at `_v7` audit time; the methodology is settled in the direction of zero additional callers; relaxing the trigger here would lift a one-caller projector on the strength of a methodology decision that excludes second + third callers — the worst-case YAGNI inversion. (Decision §1, alternative B; inherited from `_v2` → `_v6`.) The cadence-rule observation in Decision §3 records how the pickup cadence was unhonored; it does NOT change the lift trigger itself.
- **Do not introduce a merged-axiom-mark projector** (pending + committed in one shell entry). Already rejected by v1 Decision §1 alternative C; inherited verbatim through `_v2` → `_v6` to here. (Decision §1.)
- **Do not pre-register the participant or audience pending-axiom-mark leaves from this shell-package refinement.** Both surfaces have explicit refinement-stage decisions deferring those leaves until a concrete UX trigger arrives. Pre-registering them here inverts the WBS's dependency direction. (Decision §2, inheriting v1 → v6 Decision §2.)
- **Do not edit the four moderator-local symbols or their three consumers.** No re-comment, no signature change, no `Readonly` widen, no extraction of an adapter. The symbols stay at [`apps/moderator/src/graph/selectors.ts`](../../../apps/moderator/src/graph/selectors.ts) verbatim; the three moderator consumers (`GraphCanvasPane.tsx`, `StatementNode.tsx`, `PendingAxiomMarkBadge.tsx`) stay byte-identical. Their existing Vitest suite stays in place.
- **Do not edit any `.tji` file.** Per the refinement-writer brief, the orchestrator / closer own WBS shape. Decision §4 names the follow-up `_v8` task crisply (effort + one-line description + the same pickup cadence) so the closer can register it mechanically.
- **Do not add an ADR.** The methodology question this audit re-raises (are pending-axiom-marks a moderator-only proposal-author affordance, or do they belong on participant/audience surfaces too?) has not changed since v1 → v6; one further audit iteration's confirmation does not warrant an ADR. The cadence-rule-violation observation is a WBS-cadence policy applied to this single audit chain, not an architectural decision affecting the codebase; no ADR is warranted. If a future participant or audience leaf re-opens the methodology question, or if a future iteration of this chain decides to implement the `tj3`-enforceable cadence-gate mentioned in Decision §3, that work's refinement may amend the relevant ADR or open a new one; this audit does not.
- **Do not tighten the cadence rule on a single observation.** The v3 precedent's discipline (a two-iteration evidence window, not a single-iteration unilateral decision) carries forward to **how** the new cadence's enforcement is evaluated too. One pick-pass-1 pickup is observation, not enough evidence to escalate. Decision §3 reserves the escalation question for `_v8` if the pattern repeats.
- **Do act on the same-cadence `_v8` re-registration at `_v7`.** The `_v6` closer registered `_v7` with the batched cadence; this refinement carries the same cadence to `_v8` (with the cadence-enforcement-revisit clause) because the cadence policy itself remains the right rate — only its enforcement mechanism is observed to be prose-only-bounded. (Decision §4.)
- **No Playwright / Cucumber coverage added.** Pure documentation deliverable; no protocol seam crossed, no projector output changed, no user-visible behavior shift. The UI-stream e2e policy in the refinement-writer brief does not apply (this is not a UI-stream leaf and creates no new component or event surface). The backend / projector / methodology-engine guidance does not apply (projector output and wire format unchanged — no Cucumber pin warranted).

## Acceptance criteria

- [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v7.md`](extract_pending_axiom_mark_projector_v7.md) (this file) lands in the same commit as the `complete 100` marker on `shell_package.extract_pending_axiom_mark_projector_v7` in [`tasks/27-shell-package.tji`](../../27-shell-package.tji). The file contains: the audit of both preconditions, the structural reason both remain unmet AND principledly absent, the unchanged asymmetry with [`extract_votes_by_facet_projector_v2`](extract_votes_by_facet_projector_v2.md)'s relaxable trigger, the rejection rationale for relaxed-policy / merged-projector / pre-registered-future-leaf alternatives, the cadence-rule-violation observation with its structural explanation (Decision §3), and the crisp registration of the follow-up `_v8` task that Decision §4 names (with the same pickup cadence and the cadence-enforcement-revisit clause).
- **No new code in `packages/shell/src/`.** A `grep` of [`packages/shell/src/`](../../../packages/shell/src/) for active (non-comment) references to `PendingAxiomMark` / `projectPendingAxiomMarks` / `groupPendingAxiomMarksByNode` / `EMPTY_PENDING_AXIOM_MARKS` / `pending-axiom-marks` returns zero results. The incidental comment reference at [`facet-pill/participant-color.ts:2`](../../../packages/shell/src/facet-pill/participant-color.ts#L2) is allowed (it documents downstream component naming, not active code).
- **No edits to the moderator-local symbols.** `git diff HEAD` on [`apps/moderator/src/graph/selectors.ts`](../../../apps/moderator/src/graph/selectors.ts) is empty for this commit; the projector body, the grouper body, the `PendingAxiomMark` interface, and the `EMPTY_PENDING_AXIOM_MARKS` constant stay byte-identical.
- **No edits to the moderator's consumers.** `git diff HEAD` on [`apps/moderator/src/graph/GraphCanvasPane.tsx`](../../../apps/moderator/src/graph/GraphCanvasPane.tsx), [`apps/moderator/src/graph/StatementNode.tsx`](../../../apps/moderator/src/graph/StatementNode.tsx), and [`apps/moderator/src/graph/PendingAxiomMarkBadge.tsx`](../../../apps/moderator/src/graph/PendingAxiomMarkBadge.tsx) is empty.
- **No edits to `packages/shell/src/index.ts`.** No new pending-axiom-marks re-export block.
- **One follow-up named-future-task is crisply named in Decision §4**, with effort + one-line description + the same batched-audit pickup cadence semantics as `_v7` AND the cadence-enforcement-revisit clause, ready for the closer to register in [`tasks/27-shell-package.tji`](../../27-shell-package.tji) (deferred to: `shell_package.extract_pending_axiom_mark_projector_v8` — closer registers in WBS).
- **No new participant or audience leaves are scoped from this refinement.** Decision §2 inherits v1 → v6's explicit decision to leave materialization of those surfaces' pending-axiom-mark callers to their own work-streams. The future `_v8` task's lift trigger references the unchanged "both UI leaves `complete 100`" condition; its pickup cadence carries the same batched semantics (Decision §3); neither names specific candidate participant or audience leaves.
- **No tightening of the cadence enforcement mechanism implemented.** Decision §3 records the cadence-rule-violation observation and names two possible escalation paths (a `tj3`-enforceable gate task; a non-leaf "watcher" pattern) but does NOT implement either. The escalation question is reserved for `_v8`'s refinement if the violation pattern repeats. No new `.tji` task structure is introduced today.
- **TaskJuggler validity preserved.** `tj3 project.tjp 2>&1 | grep -iE "error|fatal"` returns silent after the closer's `complete 100` + named-future-task `_v8` registration (the pre-commit hook enforces this per [`docs/dev-environment.md`](../../../docs/dev-environment.md)).
- **No throwaway verification scripts.** Per ADR 0022, this audit's deliverable is the refinement document itself; the moderator's existing Vitest suite ([`apps/moderator/src/graph/selectors.test.ts`](../../../apps/moderator/src/graph/selectors.test.ts) `projectPendingAxiomMarks` + `groupPendingAxiomMarksByNode` describe blocks) continues to pin the moderator surface's local behavior unchanged. No new test, no smoke script, no executable artifact.
- **No Playwright / Cucumber scoped.** Pure documentation; no behavior change at any seam. The UI-stream e2e policy does not apply (not a UI-stream leaf; no new component / event surface). The projector / methodology-engine guidance does not apply (projector output unchanged).
- **Status block records audit outcome AND the cadence-rule-violation observation AND the `_v8` re-registration.** When the closer appends the `## Status` block, it records: "audit complete; both preconditions still unmet, bit-for-bit identical to `_v6` findings one orchestrator-pick pass earlier (no participant pending-axiom-mark rendering leaf exists or is scheduled; no audience pending-axiom-mark rendering leaf exists or is scheduled); the unchanged asymmetry with `extract_votes_by_facet_projector_v2` is recorded under Decision §1; cadence-rule pickup signals (i) and (ii) both unmet at pick time, observation recorded under Decision §3 (prose-only cadence in `.tji` `note` blocks has no `tj3`-enforceable scheduling effect); lift deferred to `shell_package.extract_pending_axiom_mark_projector_v8` with same batched-audit pickup cadence + cadence-enforcement-revisit clause (Decision §4)." Prior sections (Decisions, Inputs/context) stay untouched per the [`tasks/refinements/README.md`](../README.md) ritual.

## Decisions

### §1 — Both preconditions remain unmet AND principledly absent; defer again (do NOT relax the lift trigger on the sibling `_v2` lift's precedent)

The predecessor `_v6` audit ([`extract_pending_axiom_mark_projector_v6.md`](extract_pending_axiom_mark_projector_v6.md) Decision §4) registered THIS `_v7` task with the trigger *"fires when **both** … (a) a participant pending-axiom-mark rendering leaf … is `complete 100`, AND (b) an audience pending-axiom-mark rendering leaf … is `complete 100`,"* identical to v1 → v5's registrations of `_v2` → `_v6`. The audit at refinement time finds neither leaf exists nor is scheduled, and **the principled-absence the v1 → v6 audits all recorded has not been revised at either surface**:

- **Participant surface — still committed-only.** [`part_axiom_mark_decoration.md:27`](../participant-ui/part_axiom_mark_decoration.md#L27) stance is unchanged; zero participant code consumes the projector; zero task identifier in [`tasks/40-participant-ui.tji`](../../40-participant-ui.tji) names a future participant pending-axiom-mark rendering leaf. The refinement's Status line ("Done — 2026-05-17") has no amendment. The `part_axiom_mark_from_tablet` subtree (`complete 100`, committed-only) and the `part_pending_proposals` task (shipped a list-view pane) are both scope-disjoint from pending-axiom-mark **graph-canvas** rendering.
- **Audience surface — still committed-only.** [`aud_axiom_mark_decoration.md:38`](../audience/aud_axiom_mark_decoration.md#L38) stance is unchanged; zero audience code consumes the projector; the prose-named future polish leaf (`aud_axiom_mark_pending_render`, ~1d) remains explicitly NOT pre-registered. The refinement's Status line ("Done — 2026-05-28") has no amendment. The `audience.aud_graph_rendering` subtree (30/52 `complete 100`) has no leaf scoping pending-axiom-mark graph-canvas rendering; its remaining deferred leaves scope pending-proposals pane + annotation rendering instead.

The asymmetry with [`extract_votes_by_facet_projector_v2.md`](extract_votes_by_facet_projector_v2.md) (settled 2026-05-28 at commit `8c7087b`) is unchanged from `_v2` → `_v6`'s recording:

- The votes-by-facet projector had two callers with a methodology-shaped divergence. Resolving the methodology produced two existing callers in lockstep. The "third workspace" criterion was the weak link; relaxing it cost nothing because the strength-of-evidence was already at two byte-identical callers.
- The pending-axiom-marks projector has ONE caller — by design. The participant + audience surfaces have explicitly decided not to render pending-axiom-marks. There is no in-flight methodology task; the methodology has already concluded that pending-axiom-marks belong at the proposal-author surface only. Relaxing the trigger here means lifting a one-caller projector into shell on the strength of a methodology decision that says no second caller exists.

Three options for what to do this turn (on the lift trigger; the cadence-rule observation is Decision §3):

- **(A — chosen)** Defer again. Mark this `_v7` leaf `complete 100`; register `_v8` with identical lift-trigger semantics (both UI leaves `complete 100`). The lift continues to wait for both UI surfaces to revise their principled-absence decisions and ship concrete rendering. Reasoning: the trigger is binary on UI work that is not scheduled and is not in-flight; deferring against an unscheduled trigger is the right call exactly because the alternative (lifting on the strength of a methodology decision that excludes the additional callers) is worst-case YAGNI inversion. The audit is part of the deliverable for this iteration; the cadence-rule observation (Decision §3) and the same-cadence re-registration (Decision §4) are the rest; the lift is the deliverable when (and if) the principled-absence reverses.
- **(B)** Relax the lift trigger on the votes-by-facet precedent — lift now, with one workspace caller (moderator), under the rationale that "the projector's coupling to ADR 0030 §9 makes it shell-shaped" or "the recipe is mechanically trivial." Cost: introduces a single-caller shell entry with one workspace user — the anti-pattern the v1 audit Decision §1 alternative B already rejected on the same reasoning; the sibling `_v2`'s relaxation is not a precedent for THIS lift because the structural conditions are inverted (there: methodology-divergence resolution producing two convergent callers; here: methodology-explicit principled-absence preventing additional callers). The strength of the votes-by-facet evidence was two byte-identical post-alignment bodies; here there is one body. Rejected (same reasoning as v1 → v6 Decision §1 alternative B).
- **(C)** Defer indefinitely AND drop the `_v8` registration — let this leaf close with no follow-up, and let any future re-opening of the participant or audience stance trigger a fresh `tasks/27-shell-package.tji` task from scratch. Cost: loses the citable audit chain that links every iteration's findings; the orchestrator's tech-debt registration policy asks for a crisp future-task; dropping the registration violates that policy. Future implementers re-discovering the deferral from cold would have to re-derive the recipe + the rejection of relaxation alternatives without the audit chain to reference. Rejected (same reasoning as v1 → v6 Decision §1 alternative C). The cadence policy in Decision §4 addresses the audit-cost concern that motivated this alternative without losing the chain.

Chosen: (A). The three-caller policy floor is three; the current caller count is one; the methodology's authority for additional callers is exclusionary, not enabling. The natural path is to continue waiting until the floor is reached by a genuine revision at the UI work-streams, then lift via the same recipe `extract_cytoscape_projectors`, `shell_axiom_marks_extraction`, and `extract_votes_by_facet_projector_v2` followed.

### §2 — Do not pre-register participant / audience pending-axiom-mark leaves from this shell-package refinement (inherited from v1 → v6 Decision §2; restated for the `_v7` audit cadence)

The v1 → v6 audits' Decision §2 rejected pre-registering `part_pending_proposals.*` graph-canvas overlay leaves or the audience `aud_axiom_mark_pending_render` leaf from a shell-package refinement, on the grounds that shell-package refinements depend on UI surfaces materializing callers (not the other way around) and that the audience's own refinement explicitly framed its future polish leaf as "registered only when a concrete trigger materializes." The same reasoning applies one further audit-iteration later; both surfaces' explicit decline-to-register decisions remain unrevised.

Three options:

- **(A — chosen)** Do NOT pre-register `part_pending_proposals.*` graph-canvas additions or `aud_axiom_mark_pending_render` from this refinement. The future `_v8` task's lift trigger references the unchanged "both UI leaves `complete 100`" condition; the specific candidate leaves are named by the participant + audience work-streams when (and if) they reach the source-of-debt stage. Reasoning: inherits v1 → v6 Decision §2 verbatim; the principled-absence is durable across seven audit iterations, so re-asking the question at the batched cadence is the right rate — not pre-registering them.
- **(B)** Pre-register one or both leaves now to anchor the future lift's `depends` edges. Cost: contradicts both predecessor UI refinements' explicit decline-to-register decisions; shell-package refinement scopes UI work; introduces speculative leaves on the WBS that may never fire. Rejected (same reasoning as v1 → v6 Decision §2 alternative B).
- **(C)** Pre-register a single combined cross-surface leaf bundling participant + audience pending-axiom-mark rendering as one ~2d ticket. Cost: tangles two surfaces' independently-arrived-at design decisions; bundling produces a leaf neither work-stream owns. Rejected (same reasoning as v1 → v6 Decision §2 alternative C).

Chosen: (A). The future `_v8` task's trigger reads the actual condition ("both UI leaves `complete 100`"); the path to satisfaction is held by the audience and participant work-streams' own future design.

### §3 — Cadence-rule pickup signals were unmet at pick time; record the observation, identify the structural reason, do NOT tighten on a single observation

The `_v6` Decision §3 + §4 named two pickup signals for `_v7`: (i) 4 or more orchestrator-pick passes elapsed since `_v6` close, OR (ii) a commit landed touching one of the precondition-bearing files with a matching pending-axiom-mark identifier. The orchestrator's pick-task pass surfaced `_v7` on the **first** pick after `_v6` closed — a state in which:

- **Signal (i) — unmet.** 1 pass < 4 passes. The pickup horizon Decision §3 of `_v6` named has not been reached.
- **Signal (ii) — unmet.** The only commit since `_v6` close is `42ad04b` itself, which touched [`tasks/27-shell-package.tji`](../../27-shell-package.tji) and [`tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v6.md`](extract_pending_axiom_mark_projector_v6.md). Neither file is in the precondition-bearing list the cadence-(ii) signal scopes (`apps/{participant,audience}/src/`, `tasks/{40-participant-ui,50-audience-and-broadcast}.tji`, `tasks/refinements/{participant-ui/part_axiom_mark_decoration,audience/aud_axiom_mark_decoration}.md`) — that list intentionally excludes shell-package's own files to prevent the chain's own close-commits from looping back as pickup signals (an infinite-loop risk the `_v6` Decision §3 alternative A formulation correctly avoided by listing only the precondition-bearing surfaces).

Both signals unmet; pick happened anyway. The structural reason is that the cadence is documented in prose-and-`note` form on the `_v7` task block ([`tasks/27-shell-package.tji:301-312`](../../27-shell-package.tji#L301)) — it is human-readable narrative addressed to refinement-writers, NOT a `tj3`-enforceable structural constraint addressed to the scheduler. The orchestrator's pick-task pass walks `complete 100` markers and `depends` edges, picking the next leaf whose `depends` edges are all `complete 100`. With `_v6` `complete 100` and `_v7`'s only `depends` edge being `!extract_pending_axiom_mark_projector_v6`, `_v7` is mechanically pickable as soon as `_v6` closes. The cadence-policy text in the `note` block does not slow the scheduler — it documents intent for the refinement-writer reading the task at pickup time.

Four options for what the refinement does with this observation:

- **(A — chosen)** Record the observation, identify the structural reason (prose-only cadence in `.tji` `note` blocks has no `tj3`-enforceable scheduling effect), name two possible escalation paths for cadence enforcement (described below), and decide NOT to escalate on a single observation. The v3 precedent's discipline (a two-iteration evidence window, not a single-iteration unilateral decision) carries forward to **how** the new cadence's enforcement is evaluated too. One pick-pass-1 pickup is observation, not enough evidence to escalate the enforcement mechanism. `_v8` carries the same cadence forward (Decision §4) with a cadence-enforcement-revisit clause: if `_v8`'s pickup also violates the cadence (another pick-pass-1 or pick-pass-2 pickup with signal (ii) unmet), `_v8`'s refinement re-evaluates whether to escalate. Reasoning: tightening on a single observation risks over-correction; the precedent's evidence-gathering discipline is the right rate for cadence-policy decisions across this chain.
- **(B)** Tighten the cadence rule immediately — e.g., shorten the pickup horizon to 2 passes (over-correcting) or, more sharply, implement one of the enforcement-escalation paths in this refinement. Cost: contradicts the v3 precedent's two-iteration-window discipline; risks over-correcting on a single observation; mixes observation work with implementation work in the same iteration. Rejected.
- **(C)** Drop the cadence rule from `_v8`'s registration — return to per-pass cadence. Cost: undoes the `_v6` closure's actual decision on the strength of one violation observation; the cadence-rate change in `_v6` was correctly scoped per the v3 precedent and remains the right rate; the issue is enforcement mechanism, not rate. Rejected.
- **(D)** Implement a `tj3`-enforceable cadence-gate task structure now — e.g., introduce a `shell_package.extract_pending_axiom_mark_projector_v7_cadence_gate` non-leaf task that holds `_v8`'s `depends` edge open via a structural delay (or split `_v8`'s `depends` into multiple gate tasks that close on their own schedule). Cost: introduces meaningful WBS-structure work in the same iteration as the observation — out of this leaf's scope per the refinement-writer brief; doing it in the same iteration mixes observation with implementation, against this leaf's 0.5d budget; and the implementation is not yet justified by repeated evidence. Rejected at this iteration; named here as a possible escalation path for `_v8`'s refinement to consider if the pattern repeats.

Chosen: (A). Observation recorded; structural reason identified; escalation paths named but not implemented; `_v8` carries the same cadence forward with a cadence-enforcement-revisit clause.

Two named escalation paths (for `_v8` or later if the pattern repeats — NOT implemented today):

1. **`tj3`-enforceable cadence-gate task.** Introduce a non-leaf task (e.g., `shell_package.extract_pending_axiom_mark_projector_v8_cadence_gate`) whose `complete 100` is delayed by structural means — either a calendar-date binding (rejected by the chain's prior Decision §3 alternative D recordings, so not really an option here) or a multi-step dependency chain that consumes N orchestrator-pick passes before allowing `_v8` to surface. Implementation cost: ~0.5-1d; introduces speculative WBS structure that the orchestrator pick-task pass may or may not honor depending on how the chain is shaped. Risk: the same prose-vs-`tj3`-enforceable gap may reappear at a higher abstraction level if the gate task's own logic is `note`-only.
2. **Non-leaf "watcher" pattern.** Fold the audit-iteration chain into a single long-lived non-leaf task whose sub-tasks are picked on calendar-date or commit-signal triggers managed outside `tj3` (e.g., by a shell-side script the orchestrator's brief calls before each pick-pass). Implementation cost: ~0.5-1.5d; introduces a meta-policy mechanism that other chains could reuse but that does not exist today; requires a brief amendment to the orchestrator's pick-task workflow. Risk: introduces a new abstraction whose value is amortized only across multiple chains adopting similar policies.

Neither path is implemented in `_v7`. Both are documented here so that when `_v8` (or `_v9`, or whichever iteration first sees a repeated cadence violation) makes the escalation decision, the implementation options are pre-named and the trade-offs are pre-described.

Note on what the cadence-rule-violation observation does NOT change:

- The **lift trigger** is unchanged — `_v8` still fires the lift only when both UI leaves are `complete 100`.
- The **rejection of relaxation alternatives** is unchanged — the same v1-Decision-§1 reasoning continues to govern.
- The **shell directory layout, the symbol list, and the recipe** are unchanged — when the lift fires, it follows `shell_axiom_marks_extraction` + `extract_cytoscape_projectors` + `extract_votes_by_facet_projector_v2` verbatim.
- The **cadence rate** is unchanged — `_v8` adopts the same batched cadence (every 4 pick-passes OR a precondition-touching commit, whichever comes first). Only the enforcement mechanism's prose-only-bound is recorded; the rate itself is reaffirmed by Decision §4.

### §4 — Register one follow-up named-future-task: `shell_package.extract_pending_axiom_mark_projector_v8` (same batched cadence + cadence-enforcement-revisit clause)

The audit identifies one deferred workstream (the shell-extract lift) waiting on the same binary lift trigger as v1 → `_v2` → ... → `_v7`: both UI leaves `complete 100`. A single named-future-task `_v8` captures the lift cleanly; the lift-trigger semantics are unchanged from `_v7`'s registration in `_v6` Decision §4 because the audit found no revisions to the principled-absence at either UI surface that would warrant sharpening or relaxing the trigger. The **pickup cadence** for `_v8` is the same as `_v7`'s (per Decision §3 above): batched at every 4 orchestrator-pick passes OR upon a precondition-touching commit, whichever comes first. The **cadence-enforcement-revisit clause** is new at `_v8`: if `_v8`'s pickup also violates the cadence (pick-pass-1 or pick-pass-2 pickup with signal (ii) unmet — i.e., a second consecutive cadence violation), `_v8`'s refinement re-evaluates whether to escalate the cadence enforcement mechanism per the two paths named in Decision §3.

**Task (shell-package workstream, deferred):**

- **Name**: `shell_package.extract_pending_axiom_mark_projector_v8`
- **Effort**: 0.5d (the lift itself: create `packages/shell/src/pending-axiom-marks/`, lift `PendingAxiomMark` + `EMPTY_PENDING_AXIOM_MARKS` + `projectPendingAxiomMarks` + `groupPendingAxiomMarksByNode` + the consolidated Vitest suite, rewire all three or more callers' imports; same shape `shell_axiom_marks_extraction`, `extract_cytoscape_projectors`, and `extract_votes_by_facet_projector_v2` followed for their respective vocabularies). 0.5d holds for the lift because the body is unchanged; if the trigger has not fired by `_v8`'s pickup, the audit deliverable consumes ~0.25d and the remaining budget covers the cadence-enforcement-revisit re-evaluation. If the trigger fires later in a state where a single shared private loop can serve both moderator + participant + audience consumers (analogous to the votes-by-facet `walkVotes` private helper), the lift's refinement makes that call at its own refinement time.
- **Description**: "Lift `projectPendingAxiomMarks` + `groupPendingAxiomMarksByNode` + `PendingAxiomMark` + `EMPTY_PENDING_AXIOM_MARKS` from `apps/moderator/src/graph/selectors.ts` into `packages/shell/src/pending-axiom-marks/` once BOTH a participant pending-axiom-mark rendering leaf AND an audience pending-axiom-mark rendering leaf land (three-caller convergence). The participant and audience surfaces have explicitly declined to render pending-axiom-marks ([`part_axiom_mark_decoration.md:27`](../participant-ui/part_axiom_mark_decoration.md#L27), [`aud_axiom_mark_decoration.md:38`](../audience/aud_axiom_mark_decoration.md#L38)); this lift fires only after both surfaces revise that decision and ship concrete rendering. Seven audit iterations (v1, v2, v3, v4, v5, v6, v7 — all settled 2026-05-28 → 2026-05-29) confirmed the trigger remains unmet AND principledly absent at each cadence. `_v7` observed that the new batched cadence the `_v6` closer scoped was unhonored at pick time (prose-only cadence in `.tji` `note` blocks has no `tj3`-enforceable scheduling effect); `_v8` adopts the same batched cadence as `_v7` (pick when 4 orchestrator-pick passes have elapsed since `_v7` close OR a precondition-touching commit lands, whichever comes first) AND inherits a cadence-enforcement-revisit clause: if `_v8`'s pickup also violates the cadence (pick-pass-1 or pick-pass-2 pickup with the commit signal also unfired — a second consecutive violation), `_v8`'s refinement re-evaluates whether to escalate the cadence enforcement mechanism per `_v7` Decision §3's two named escalation paths (a `tj3`-enforceable cadence-gate task; a non-leaf 'watcher' pattern)."
- **Home**: [`tasks/27-shell-package.tji`](../../27-shell-package.tji) (shell-package surface).
- **Lift trigger** (unchanged from v1 → v7): fires when **both** of the following hold: (a) a participant pending-axiom-mark rendering leaf (likely under a `part_pending_proposals.*` group, per [`part_axiom_mark_decoration.md:27`](../participant-ui/part_axiom_mark_decoration.md#L27)) is `complete 100`, AND (b) an audience pending-axiom-mark rendering leaf (likely `aud_axiom_mark_pending_render`, per [`aud_axiom_mark_decoration.md:38`](../audience/aud_axiom_mark_decoration.md#L38)) is `complete 100`. Both leaves must verbatim-port the moderator's projector (or a documented superset thereof); the three-caller convergence check is what makes the lift safe.
- **Pickup cadence** (same as `_v7`): batched-audit cadence with a precondition-touching-commit pickup signal per Decision §3 + `_v6` Decision §3. The orchestrator's pick-task pass should surface `_v8` only when one of: (i) 4 or more orchestrator-pick passes have elapsed since `_v7` close, or (ii) a commit lands touching the precondition-bearing files with a matching pending-axiom-mark identifier (per the grep specification in `_v6` Decision §3 + this refinement's Decision §3). The (ii) signal is the meaningful change-detector; the (i) signal is the calendar-style sweep ceiling. **Note**: the cadence is documented in prose, not `tj3`-enforceable; the orchestrator's pick-task pass may pick `_v8` before either signal fires (as it did for `_v7`). If the violation pattern repeats, the cadence-enforcement-revisit clause activates.
- **Cadence-enforcement-revisit clause** (new at `_v8`): if `_v8`'s pickup also violates the cadence (either pick-pass-1 or pick-pass-2 pickup, with signal (ii) also unfired), `_v8`'s refinement re-evaluates whether to escalate the cadence enforcement mechanism. Two escalation paths are pre-named in `_v7` Decision §3: (1) a `tj3`-enforceable cadence-gate task; (2) a non-leaf "watcher" pattern requiring a brief amendment to the orchestrator's pick-task workflow. `_v8`'s refinement chooses among (1), (2), or "hold the prose-only cadence as documentation-only and accept that the orchestrator's pick-task pass picks every successor-of-closed-leaf on the first pass" based on the two-iteration evidence (the `_v7` observation + whatever `_v8`'s pickup shows).
- **Depends on**: `shell_package.extract_pending_axiom_mark_projector_v7` (`!`-edge, settled here). The two future UI leaves' edges are added when those leaves materialize; the future-task's dependency declaration is updated at that point. Transitively the chain is `extract_cytoscape_projectors` → `extract_pending_axiom_mark_projector` → `_v2` → `_v3` → `_v4` → `_v5` → `_v6` → `_v7` → `_v8`.
- **Source of debt**: this refinement, Decisions §1-§3.

Two reasons for the `_v8`-suffix rather than reusing the existing `_v7` leaf's name (same rationale as v1 → v6 Decision §4 used for their `_v2`- → `_v7`-suffix choices):

- The `_v7` leaf has a fixed identity in the WBS history (it was registered by `_v6` Decision §4; its `complete 100` closes this audit and records the cadence-rule-violation observation). Reusing the same name for the next audit would be ambiguous in `tj3` history and in cross-refinement citations.
- Each audit iteration corresponds to one pickup of this chain and produces its own audit refinement. The `_vN`-suffix chain records the cadence cleanly; readers see at a glance how many iterations the deferral has held without the trigger firing. The new cadence-enforcement-revisit clause at `_v8` is an iteration-level decision, NOT a suffix-scheme change.

Three rejected alternatives:

- **No new task** — let the existing `_v7` stay open. Cost: orchestrator pick-task queue keeps re-picking this leaf; the audit's value is lost to "in progress" status; closer ritual cannot fire `complete 100`. Rejected (same reasoning as Decision §3 alternative B).
- **One combined task** that bundles the future participant leaf, the future audience leaf, and the lift itself. Cost: same as v1 → v6 Decision §4's rejected alternative A (tangles three workstreams under one owner; violates the WBS's per-area-task-file split; the lift cannot land until the two UI leaves land, so combining them produces a leaf that blocks on its own components). Rejected.
- **Drop the audit-iteration cadence entirely** — defer the registration of any future task until one of the two UI leaves materializes (i.e., close `_v7` with no `_v8`). Cost: loses the audit-chain's citable WBS home (a future implementer must rediscover the deferral chain from closed-leaf history rather than from an open named-future-task); contradicts the orchestrator's tech-debt registration policy ("name the future task crisply"). The cadence policy in Decision §4 addresses the per-pass cost concern that motivated this alternative without losing the chain; the cadence-enforcement-revisit clause addresses the cadence-violation concern that another reader might motivate it with. Rejected (same reasoning as v1 → v6 Decision §4 alternative C, sharpened by Decision §3's observation + the cadence-enforcement-revisit clause resolving the enforcement question for the next iteration).

Chosen: one named-future-task `_v8`, named and effort-estimated as above, ready for the closer to register mechanically. The `_v8` task inherits the unchanged lift trigger AND the same batched-audit pickup cadence AND the new cadence-enforcement-revisit clause; its refinement (when pickup fires) will document which of the two pickup signals fired (if any), re-run the precondition audit, and — if a second consecutive cadence violation is the outcome — re-evaluate the cadence enforcement mechanism per Decision §3's two named escalation paths.

## Open questions

(none — all decided)

## Status

**Done** — 2026-05-29.

- Audit complete: both preconditions still unmet, bit-for-bit identical to `_v6` findings one orchestrator-pick pass earlier — no participant pending-axiom-mark rendering leaf exists or is scheduled (`tasks/40-participant-ui.tji`; `apps/participant/src/` grep: zero matches); no audience pending-axiom-mark rendering leaf exists or is scheduled (`tasks/50-audience-and-broadcast.tji`; `apps/audience/src/` grep: zero matches).
- Moderator's four pending-axiom-mark symbols confirmed byte-identical to `_v6` snapshot: `PendingAxiomMark` (line 212), `EMPTY_PENDING_AXIOM_MARKS` (line 225), `projectPendingAxiomMarks` (line 251), `groupPendingAxiomMarksByNode` (line 303) in `apps/moderator/src/graph/selectors.ts`.
- No `packages/shell/src/pending-axiom-marks/` directory created; no symbol moved; no caller rewired; no shell `index.ts` change.
- Cadence-rule-violation observation recorded under Decision §3: the batched-audit cadence scoped by `_v6` Decision §3+§4 (pick when ≥4 passes elapsed OR a precondition-touching commit lands) was unhonored — `_v7` was picked at pick-pass 1 with signal (ii) also unfired; structural reason is that prose-only cadence in `.tji` `note` blocks has no `tj3`-enforceable scheduling effect. Observation does NOT tighten the lift trigger; cadence-enforcement-revisit deferred to `_v8` if the pattern repeats.
- `shell_package.extract_pending_axiom_mark_projector_v8` registered in `tasks/27-shell-package.tji` with same lift trigger (both UI leaves `complete 100`), same batched-audit pickup cadence (≥4 passes OR precondition-touching commit), and the new cadence-enforcement-revisit clause (Decision §4).
- Refinement file landed: `tasks/refinements/shell-package/extract_pending_axiom_mark_projector_v7.md`.
