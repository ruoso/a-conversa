# @a-conversa/audience

Audience surface frontend (React).

Scaffolded by `foundation.repo_skeleton.dir_layout`; real code lands with subsequent tasks. The replay viewer / test-mode surface (originally sketched as `apps/replay`) is deferred — it will either land here or as its own workspace once the replay refinement settles.
