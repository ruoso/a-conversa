{
  "name": "@a-conversa/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "migrate": "tsx scripts/migrate.ts",
    "migrate:create": "node-pg-migrate create --migrations-dir migrations --migration-file-language sql"
  },
  "dependencies": {
    "@a-conversa/shared-types": "workspace:^",
    "node-pg-migrate": "8.0.4",
    "pg": "8.20.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0"
  }
}
