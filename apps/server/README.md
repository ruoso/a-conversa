# @a-conversa/server

Backend service: HTTP + WebSocket server, event-log writer, projection runtime.

Scaffolded by `foundation.repo_skeleton.dir_layout`; real code lands with subsequent tasks. Migrations will live under `apps/server/migrations/` per refinement R4 (created by the migration-runner task).
