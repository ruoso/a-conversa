# @a-conversa/shared-types

TypeScript types shared between server and clients (event payloads, API contracts, node/edge shapes, WebSocket message schemas).

Scaffolded by `foundation.repo_skeleton.dir_layout`; real code lands with subsequent tasks.
