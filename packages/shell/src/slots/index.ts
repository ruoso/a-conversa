// Barrel for the shell's slot-merge subsystem.
//
// Refinement: tasks/refinements/shell-package/shared_shell_extract_merge_slots_and_derive_slot_occupants.md

export {
  SLOT_ROLES,
  deriveSlotOccupants,
  mergeSlots,
  type ParticipantRow,
  type SlotOccupant,
  type SlotOccupants,
  type SlotRole,
} from './slots.js';
